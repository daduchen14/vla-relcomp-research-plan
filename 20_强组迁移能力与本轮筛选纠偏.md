# 强组迁移能力、个人能力栈与本轮筛选纠偏 V1

> 快照：2026-08-25。本文件记录两个问题：强组为什么能迅速进入新热点；这对用户应学习什么、以及当前项目筛选意味着什么。

## 一、结论

强组看起来“转向很快”，通常不是每次从零追热点，而是：

```text
稳定的核心问题与能力
  + 已积累的数据/代码/平台/学生
  + 快速吸收一种新模型或新范式
  + 把新范式接到原有问题上
= 外界看到的快速转向
```

真正可迁移的不是某个热词，而是对问题的抽象、共同技术底座、快速实验和组织协作能力。个人不需要成为所有方向各学五年的专家，但也不能只会调用新 API；更现实的目标是 **T 型能力**：一套广泛可迁移的 AI/计算底座，加一个能做深、能产出证据的具体问题。

## 二、董豪并不是 2024—2025 才突然转做具身

官方时间线更接近“长期机器人/视觉主线逐步吸收大模型和 Agent”：

- 2019 年加入北大时，官方介绍覆盖深度学习、数据/机器创造、计算机视觉等；同期介绍已写到可泛化机械臂操作、机器人视觉与移动设备自主决策。[2019 入职介绍](https://cfcs.pku.edu.cn/news/42cfcs239210.htm)｜[深度强化学习专著介绍](https://cfcs.pku.edu.cn/news/42cfcs239174.htm)
- 2021 年其学生公开方向已是计算机视觉、机器人、强化学习。[CFCS 2021 研究生页](https://cfcs.pku.edu.cn/people/graduate_students/2021/index.htm)
- 2023 年已有机器人操作、导航、affordance、强化学习等连续成果；2024 年进一步出现使用视觉语言模型/大模型做通用指令导航、机器人操作和纠错；2025—2026 年才更集中使用具身智能、VLA、Agent、空间智能等当前术语。[董豪当前论文与新闻时间线](https://zsdonghao.github.io/)

所以更准确的主线是：

```text
视觉表示/生成与机器人感知
  → 物体、位姿、affordance 与操作
  → 强化学习、导航和闭环决策
  → 语言/多模态模型作为指令与世界知识接口
  → 具身 Agent、VLA、空间智能与规模化数据
```

表面名词变化很快，底层一直围绕“机器如何感知环境并采取行动”。Agent 并非完全陌生的新大陆，而是把观察、状态、计划、动作、反馈这条闭环用大模型重新实现。

## 三、强组的“快速转向能力”由什么构成

### 1. 稳定的问题母题

强组通常不是问“今年最火的词是什么”，而是长期问一类问题：机器人如何泛化操作、模型如何低成本推理、知识如何被检索和调用。新模型出现后，只需判断它能否突破旧瓶颈。

### 2. 可复用的抽象

很多方向共享同一套结构：

| 抽象 | LLM Agent | 具身 Agent | 软件 Agent |
|---|---|---|---|
| Observation | 文本/工具返回 | 图像、深度、触觉 | 代码、终端、测试日志 |
| State/Memory | 对话与任务状态 | 位姿、地图、历史动作 | 仓库状态、依赖、修改历史 |
| Action | 调工具/回复 | 移动、抓取、操作 | 编辑、执行命令、提交补丁 |
| Feedback | 工具结果/评分 | 成功、碰撞、奖励 | 测试通过/失败 |
| Evaluation | 任务完成率 | SR/SPL/成功率 | resolved rate/测试正确性 |

理解这些抽象以后，从数字 Agent 到具身 Agent 不是零迁移；变化主要在 observation/action 的具体表示、数据和领域约束。

### 3. 共同技术底座

Python、Linux、Git、PyTorch、深度学习、Transformer、概率/优化、数据处理、实验设计、可视化、论文阅读与复现不会因方向改名而清零。

### 4. 已有基础设施

强组拥有仿真器、机器人、数据、benchmark、训练脚本、算力和合作单位。新学生不是从空文件夹起步，而是在一条已有生产线上增加一段。

### 5. 团队而不是单人全知

老师、博士、硕士、工程师和公司合作者可以并行分工。外界看到某位 PI 一年覆盖很多主题，不代表 PI 逐行实现所有仓库、也不代表一个新人应独自学完所有方向。

### 6. 研究品味与快速否定

更稀缺的能力是判断：新范式解决了真问题还是只换包装；什么实验能最快证伪；何时停止一个没有信号的切口。这也是本次先做撞题和失败边界审计的原因。

## 四、用户需要的不是“先学完全部 AI”

对用户更合适的是五层能力栈：

1. **计算底座**：Python、Linux、Git、数据结构、调试与基本工程。
2. **机器学习底座**：线性代数、概率、优化直觉、PyTorch、训练/验证/过拟合、常见评价指标。
3. **现代模型底座**：神经网络、Transformer、token/embedding/attention、预训练—SFT—偏好/RL 后训练的基本链条；多模态时再补视觉编码与对齐。
4. **Agent/闭环抽象**：观察、状态、记忆、动作、工具、反馈、规划和评测。它可迁往软件 Agent、RAG、具身、自动实验等多种场景。
5. **一个方向的深度**：例如视觉—语言—动作、后训练与可靠性、模型推理系统等；只选一个问题做出真正对照和消融。

真正抗周期的元能力是：

- 一周内把陌生论文画成输入—模块—输出—指标；
- 两周内跑出可信缩小基线；
- 把失败拆成可验证的假设；
- 只学解决当前瓶颈需要的理论，之后再扩展；
- 能解释结论为何成立、在哪里不成立。

## 五、“先遇到具体问题再学”不是科研弱点

用户偏好“发现真实需求—理解问题—解决问题”，这与实验型科研很相容。适合的项目叙事应是：

```text
具体场景发生了什么失败
  → 为什么现有方法解决不好
  → 哪个原因可以被单独验证
  → 做最小干预
  → 比较结果与失败边界
```

这比先背一堆 SFT、RLHF、VLA 名词更适合用户。需要防止的不是需求驱动，而是只选“肉眼可见的应用需求”，忽略测量、因果和已有论文；因此每个场景还要配正式证据和可量化指标。

## 六、SWE-MiniSandbox 的真实定位与降级理由

SWE-MiniSandbox 不是“设计一个更会编程的 AI 模型”，而是 **软件 Agent 的执行基础设施与评测可靠性**：

- Agent 模型本身多数作为上游或黑箱；
- 项目核心是 Linux namespace/chroot、依赖环境、测试 harness、false-pass/false-fail 和系统测量；
- Transformer、深度学习和多模态不是主要工作量；
- 它的长期资产偏操作系统、软件测试和 AI 系统，而不是模型训练能力。

它此前居首，是因为书面 70 天成功率高、ICML 2026 关联直接、问题可测，不是因为它最符合用户的长期身份。用户现已明确：对这种工作形态兴趣偏低，担心知识过窄、与广义 AI/内容传播退路不匹配。按照“明确反感/低兴趣可否决”规则，它应从默认主线降为方法参考或系统备用。

## 七、NavSpace 的真实定位

NavSpace 属于 **具身导航 + 多模态 Agent 评测**，但当前设计的用户项目不是：

- 训练一套通用 VLA；
- 做机械臂抓取；
- 搭建真机系统；
- 把整个具身智能学完。

它研究“导航 Agent 为什么失败、评测是否把接口失败算成空间能力失败”。这比 SWE 更有具体场景和视觉反馈，也会接触多模态、Agent 闭环和仿真；但它仍偏 benchmark/诊断，不等于具身智能所有主线。如果用户真正想解决“机器人能力不足”而不是“考试怎样归因”，需要重新设计方法型切口，不能假装 NavSpace 已代表全部具身项目。

## 八、当前到底收敛到了哪一层

没有得出“大模型、多模态、体系结构都不做”的结论。准确状态是：

| 层级 | 当前状态 |
|---|---|
| 大方向 | LLM/后训练、Agent、多模态、具身、AI 系统仍在方向空间内 |
| 五篇代表工作 | 对 SWE、PQCache、NavSpace、Fine-R1、MagCache 完成了一轮具体项目审计 |
| 被淘汰的东西 | PQCache 的“自适应预算”、Fine-R1 的“视觉证据扰动”、MagCache 的“再做动态缓存”等**具体切口** |
| 暂留的具体切口 | SWE 环境语义审计、NavSpace 协议归因、MagCache 尾部失效 |
| 用户反馈后的变化 | SWE 不再是默认主线；NavSpace 是候选样例，不是最终方向 |
| 尚未完成 | 按用户实际报考学院重新限定教师池，并为多模态/后训练/具身能力改进寻找新的不撞题切口 |

所以这轮不是“最后只剩两个方向”，而是用两个具体案例验证了一套更可懂的筛选方法，同时暴露了两个新约束：长期 AI 能力/传播价值，以及培养单位资格。

## 九、培养单位是新的硬门槛

若王选所不是用户可报考或希望进入的培养单位，则王选所教师不能因论文、仓库或公开实习入口而获得“目标导师直接性”分。今后顺序必须改成：

```text
先确认用户实际报考学院/专业与可选导师范围
  → 再在范围内看强组与项目
  → 外院教师只作合作生态/方向参考
```

此前王选所项目偏多，不代表对其特别偏爱，主要来自三个偏差：其公开主页/仓库证据较完整；A2 候选里王选所 LLM/Agent artifact 较多；评分过度奖励“有仓库、能实习、能在 70 天闭环”，却没有先把培养单位资格设成硬门槛。这一偏差现已明确记录。

## 十、被用户认可的交付形式

后续每轮候选都采用：

1. 告诉用户各选项是什么；
2. 说明为什么看好/不看好；
3. 展示统一维度和权重；
4. 给一个零门槛具体场景；
5. 区分事实、推断、假想例子和实测；
6. 用户先反馈工作形态，不要求伪造专业偏好；
7. 新反馈可以推翻旧排名，保留版本和原因；
8. 只有概念上愿意继续的项目才进入付费执行测试。

这比一次性输出“方向排名”更适合跨专业用户，也应作为未来产品化的核心交互协议。

