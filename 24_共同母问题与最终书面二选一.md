# 共同母问题与阶段性排序 V2（已由 D1 取代）

> 快照：2026-08-25。本文保留两项进入最终比较前的状态；正式决定以[25_正式项目决策D1_VLA-RelComp.md](./25_正式项目决策D1_VLA-RelComp.md)为准。未运行仓库。

## 一、用户的理解是对的

两个项目都在问：

> 模型的正确行为是否建立在真正相关的信息和可迁移结构上，还是只利用了训练数据中的捷径、偶然相关性或记忆模板？

“偷奸耍滑”是一个很形象的直觉，但技术上更常写成：

- **shortcut learning**：学到捷径；
- **spurious correlation**：依赖偶然相关性；
- **grounding**：回答或动作是否扎根于真实视觉证据；
- **compositional generalization**：已学部件能否重新组合；
- **distribution shift / OOD generalization**：训练分布变化后能否保持能力；
- **calibration / selective prediction**：证据不足时是否知道自己不可靠。

科研上不能直接证明模型“真正理解”了。能做的是设计受控变化：保留任务含义、改变无关线索，或把已知部件换成新组合；如果行为仍稳定，才获得更强的泛化证据。

## 二、两项的区别

| 层面 | Fine-R1证据充分性 | VLA-Arena组合泛化 |
|---|---|---|
| 输入 | 一张图＋识别问题 | 连续图像＋机械臂状态＋语言指令 |
| 输出 | 一次类别判断/拒答 | 连续动作块和完整轨迹 |
| 主要检查 | 预测是否真有视觉证据 | 是否能把物体、关系和动作重新组合 |
| 错误后果 | 一次答案错或高置信幻觉 | 一个动作错会改变下一帧，错误逐步累积 |
| 研究关键词 | grounding、open-set、calibration | compositional generalization、closed-loop control |
| 额外知识 | 选择性预测、细粒度视觉 | 仿真、动作空间、模仿学习、轨迹评测 |

所以它们不是两个毫无关系的方向，而是同一个长期问题在“静态认知”和“动态行动”上的两个层次。这是优点：前者知识可以迁到后者，项目失败时也不会全部重学。

## 三、阶段性排序，不是定题

### 当前高上限候选：VLA-Arena单套件组合泛化

理由：

1. **更符合问题驱动习惯**：场景、失败和改进都能直接观察，不需要先接受抽象评分器。
2. **AI核心足够强**：不是只搭仿真；VLA同时使用视觉、语言、Transformer表示和动作生成。
3. **研究协议干净**：VLA-Arena规定L0训练、L1/L2测试，天然区分记忆和泛化；官方有安全、干扰、外推和长程分级任务。
4. **北大连接直接**：VLA-Arena是杨耀东团队的ICML 2026成果；这比把外部RoboTwin误认成北大项目更可靠。
5. **资源风险已经下降**：官方框架Apache-2.0，任务资产约850MB，支持按模型隔离环境，并明确集成SmolVLA。SmolVLA官方为450M参数，通用示例中20k步微调约需单张A100四小时；VLA-Arena具体数据耗时仍需实测，但已不是“只能8卡大模型”的路线。[VLA-Arena](https://vla-arena.github.io/)｜[VLA-Arena仓库](https://github.com/PKU-Alignment/VLA-Arena)｜[SmolVLA官方文档](https://huggingface.co/docs/lerobot/smolvla)

当前只固定研究问题，不提前拍脑袋固定方法：

> 轻量VLA在L0训练成功后，能否把已学物体、空间关系和动作组合迁到L1/L2？主要失败究竟来自视觉识别、关系表示还是低层控制？

基线后只有两种合法发展：

- 关系/对象分布不平衡明显：研究平衡采样或难例课程；
- 关系混淆明显但抓取正常：研究轻量空间子目标监督。

没有基线证据前，不同时承诺两个改动。

### 当前较稳候选：Fine-R1-3B证据充分性

保留理由：官方Stage-1 checkpoint、3B模型、数据和训练脚本齐，进入速度更快；能学习VLM、SFT、RL式后训练和可靠性评测。

降为备用的原因：

- Fine-R1本身已覆盖seen/unseen细类别和开放世界识别；
- PAPO、VAPO、SIEVES、FG-BMK等已密集研究视觉证据、扰动、忠实性和选择性回答；
- 要做出不重复的L3切口，比表面看起来更难；
- 即使技术工作可参考，导师/团队风险仍应与论文价值分开处理。

## 四、若未来选VLA-Arena，对应的14天闸门草案

### 第1—10天：共同底座

按[两个候选零门槛样例](./23_两个候选零门槛认知样例.md)的共同路线学习PyTorch、Transformer、VLM、SFT、数据和实验设计。不安装两套大型项目并行折腾。

### 第11—14天：只闯VLA-Arena（尚未批准启动）

目标按顺序执行：

1. 在正确的Linux/NVIDIA环境安装VLA-Arena；
2. 只下载一个外推套件；
3. 用官方checkpoint或SmolVLA跑通一个episode并生成日志/视频；
4. 跑最小L0/L1对照，不训练新方法；
5. 能解释一次失败属于识别、关系还是控制。

通过条件：

- 单episode可重复；
- 指标和视频能对应；
- 估计一个suite的多seed基线能在后续两周完成；
- 用户能口述输入—模型—动作—环境反馈的数据流。

退出条件：

- 正确付费环境中四天仍不能形成可重复episode；
- 官方轻量checkpoint/数据链不完整；
- 评测耗时使一个suite的可信基线无法在两周完成；
- 用户能运行却无法解释动作数据流和失败类型。

退出后不继续修一个月，直接切Fine-R1-3B；前10天共同知识保留。

## 五、当前究竟收敛到什么程度

- **方向池**：收敛到“多模态后训练”和“具身VLA”两条，不再扩展其他大方向。
- **具体候选**：VLA-Arena组合泛化与Fine-R1证据充分性，两项均通过了通俗理解门槛。
- **阶段性排序**：VLA-Arena上限和场景性更强；Fine-R1进入更快、风险相对低。这是比较结论，不是用户选择。
- **尚未完成**：两项都还缺最终项目定义、严格的新颖性边界、最小资源预算和同口径70天成功概率。
- **尚未固定**：最终平台、模型、任务、具体改进和正式开工时间。

因此现在只是从庞大方向池压到了两个可理解的具体候选。下一步应先把两项都补成同等完整的项目方案，再做一次有依据的二选一；不能因为其中一项更有展示感就直接定题。
