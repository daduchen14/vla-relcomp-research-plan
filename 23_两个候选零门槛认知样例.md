# 两个候选的零门槛认知样例 V1

> 目标：不要求用户先会VLA、RL或多模态，通过一个具体例子判断自己能否理解问题、愿不愿意沿着它学。示例中的数字是演示性设计，不是已运行结果。

## 一、候选1：VLA-Arena组合泛化

### 1. 一次任务是什么样

场景里有：红杯、蓝罐、白盘子和一个盒子。指令是：

> 把蓝罐放到盒子后面。

VLA每一步收到：

- 桌面相机图像；
- 手腕相机图像；
- 机械臂当前位置；
- 语言指令。

它输出的不是一句话，而是一小段动作，例如：机械臂向右移动、向前移动、下降、闭合夹爪、抬起、移到盒子后侧、松开。

### 2. 我们怀疑什么

模型在训练中可能见过：

- 把红杯放到盘子左边；
- 把蓝罐放到盘子右边；
- 把苹果放到盒子后面。

但没见过“蓝罐＋盒子＋后面”这个组合。

如果模型真的理解物体、容器和空间关系，它应能把已学会的部件重新组合；如果只是记动作模板，它可能抓对蓝罐，却放到盒子右边，或走到旧训练位置。

### 3. 最小实验

固定一个轻量VLA和VLA-Arena的`preposition_combinations`套件：

| 组别 | 训练 | 测试 | 目的 |
|---|---|---|---|
| L0 | 官方基础组合 | 官方L0 | 确认模型能记住并执行训练分布 |
| L1 | 仍只训练L0 | 官方L1新组合 | 检查是否会组合已有关系与对象 |
| L2 | 仍只训练L0 | 官方L2更难组合 | 检查能力边界 |

每次失败进一步记录：

1. 没抓到目标物体；
2. 抓对了，但空间关系错；
3. 放置方向对，但距离/碰撞失败；
4. 前半段正确，后续动作累积漂移；
5. 完成任务但违反安全约束。

单一成功率只能告诉我们“做没做成”，这张失败表才开始解释“为什么”。

### 4. 自己的改动可能长什么样

现在只保留两类假设，最终只能选一类：

- **数据假设**：L0中的空间关系、物体和容器组合不平衡，使模型把偶然相关性当规则。做关系—对象平衡采样或难例课程。
- **表示假设**：直接从整句指令到动作，模型没有形成稳定的“目标物体—参照物—关系”中间状态。增加一个轻量空间子目标监督，再输出动作。

不能直接把这两项都做；先用基线失败分布决定哪个假设有证据。

### 5. L1、L2、L3分别是什么

- L1：官方模型能在一个套件上跑，复现一个成功率。
- L2：得到L0/L1/L2完整曲线、失败分类、多seed和视频证据。
- L3：基线证明某类失败稳定存在；针对它做一个改动；L1/L2提高且L0不明显退化；有对照、消融和失败边界。

### 6. 三分钟答辩骨架

> 通用VLA在训练任务上表现不错，但可能依赖物体和动作模板，不能组合空间关系。我使用VLA-Arena固定L0训练、L1/L2测试的协议，选择一个轻量模型，把失败拆成目标识别、空间关系、低层控制和长程漂移。基线显示主要瓶颈后，我只针对该瓶颈修改数据采样或中间监督，并用成功率、关系混淆、多seed和失败视频验证。项目的重点不是搭机器人Demo，而是验证模型究竟学到了可组合关系还是训练模板。

### 7. 真正需要学什么

- VLM怎样把图像和文字变成表示；
- VLA怎样把表示变成连续动作或动作块；
- 模仿学习/SFT最基本训练逻辑；
- 坐标、位姿、夹爪和轨迹的直觉；
- 仿真episode、成功率、多seed和失败日志。

暂时不需要学：真机控制、SLAM、复杂动力学、从零预训练VLA、完整强化学习。

## 二、候选2：Fine-R1证据充分性

### 1. 一次任务是什么样

给模型一张鸟的图片，问它是哪一种鸟。普通模型可能回答：

> 这是红尾鵟，因为尾部呈砖红色，翅膀宽而圆。

但图片可能很模糊，尾部甚至被树枝挡住。回答听起来很专业，不代表它真的看见了这些特征。

### 2. 我们怀疑什么

Fine-R1通过CoT-SFT和TAPO让模型学习“先分析视觉特征，再比较候选类别，最后预测”。这能提高识别率，但真实使用还有另一个问题：

> 当关键证据不存在、类别不在候选集合或图像质量过低时，模型是否仍会高置信地编出一个细分类别？

这不是单纯追求更高准确率，而是研究“答对多少”和“什么时候不该乱答”的权衡。

### 3. 最小实验

从官方3B Stage-1 checkpoint起步，只选1—2个数据集：

| 输入组 | 例子 | 正确行为 |
|---|---|---|
| 正常证据 | 清楚看到鸟的花纹、嘴、翅膀 | 给出具体类别 |
| 弱证据 | 降分辨率、轻遮挡、远距离 | 置信应随证据下降 |
| 明显缺证据 | 前景被遮掉或只剩背景 | 拒答/承认证据不足 |
| unknown | 测试类别不在允许候选中 | 不应硬塞进已知类别 |

记录：准确率、错误时的置信、拒答覆盖率、同一图片多次推理的一致性，以及seen/unseen类别差异。

### 4. 自己的改动可能长什么样

先不直接发明新RL算法。按风险从低到高：

1. 用Stage-1 checkpoint建立证据强度—置信—错误的基线；
2. 加入少量“证据不足/unknown”SFT样本，比较是否只学会机械拒答；
3. 若SFT存在明确缺陷，再设计一个同时奖励正确识别和合理拒答的轻量目标；
4. 只在必要时使用缩小的TAPO/PAPO训练链。

### 5. L1、L2、L3分别是什么

- L1：官方Fine-R1-3B能在一个数据集评测。
- L2：形成证据强度、类别范围、准确率与高置信错误的完整地图。
- L3：提出一个可训练的选择策略，使相同覆盖率下高置信错误减少，并证明它不是“所有题都拒答”。

### 6. 三分钟答辩骨架

> 现有细粒度多模态模型追求必须给出具体类别，但真实图片可能缺少区分证据。我以Fine-R1-3B为基线，把输入分为正常、弱证据、缺证据和未知类别，测量准确率之外的风险—覆盖关系。基线若在证据不足时仍高置信预测，我用少量选择性训练让模型学会在可回答时识别、不可回答时拒答，并检查seen/unseen类别、不同退化程度和多seed下是否稳定。项目关注的不是让模型少回答，而是让它知道自己的视觉证据边界。

### 7. 真正需要学什么

- Transformer和VLM的最小结构；
- SFT、LoRA、checkpoint和训练/验证集；
- CoT为什么可能提高答案却掩盖视觉错误；
- 分类、open-set/unknown、置信与风险—覆盖曲线；
- 可选：RLVR/TAPO/PAPO的基本目标。

暂时不需要学：完整机器人仿真、连续控制、分布式VLA-RL。

## 三、两项共同的前10天

前10天不是分别学两套课程，大约七成公共：

| 天数 | 共同任务 | 必须产出 |
|---|---|---|
| 1—2 | Python环境、NumPy/PyTorch tensor、Dataset/DataLoader | 能读一批图像/文本并打印张量形状 |
| 3—4 | 神经网络训练循环、loss、反向传播、训练/验证/过拟合 | 在小数据上训练一个分类器并画曲线 |
| 5—6 | token、embedding、attention、Transformer；VLM视觉编码与文本解码 | 画出“图像＋指令→表示→输出”的数据流 |
| 7 | SFT、LoRA、checkpoint、推理采样 | 对小模型完成一次LoRA或理解官方脚本每一段 |
| 8 | 指标、对照、消融、多seed、数据泄露 | 为一个假想改动写出对照表 |
| 9—10 | 两个候选各读一篇主论文，只追输入—模块—输出—指标 | 分别完成一页论文结构图和三分钟口述 |

第11天才真正分叉：

- 选Fine-R1：进入细粒度数据、证据充分性和轻量后训练；
- 选VLA-Arena：进入动作空间、仿真episode和单套件评测。

这意味着即使14天后具身路线因仿真失败而退出，前10天也不会浪费，能够平移到Fine-R1或文本小模型后训练。

## 四、现在需要什么反馈

用户不需要回答任何专业概念，也不需要立即二选一。只需要阅读上面两个“任务场景”和“三分钟答辩骨架”，反馈三件事：

1. 哪一个场景读完更容易理解它在解决什么；
2. 哪一个愿意连续讲20分钟而不觉得纯属包装；
3. 哪一处仍然完全看不懂。

这三项反馈会影响先做哪一个14天闸门；它不是要求用户凭空判断论文方法。
