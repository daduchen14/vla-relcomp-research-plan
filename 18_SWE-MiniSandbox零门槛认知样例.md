# SWE-MiniSandbox 零门槛认知样例 V1

> 用时目标：45—60 分钟。目的不是学会全部 Linux，也不是证明 MiniSandbox 已有 bug，而是让零科研起点的人看懂：这个项目具体研究什么、证据长什么样、自己最后要讲什么。

## 一、先把名词全部翻译成人话

- **软件 Agent**：模型读代码、改代码、运行命令，尝试修复一个真实 bug。
- **测试**：裁判。通过通常记为“修好了”，失败记为“没修好”。
- **容器**：为每道题准备一间独立、可还原的房间，里面有指定操作系统、Python 和依赖。
- **MiniSandbox**：不为每题搬来一整套房间，而是共享一部分地基，再用 Linux 的 namespace、`chroot`、bind mount 和独立工作目录隔开任务。
- **语义一致性**：同一个代码补丁，在两种环境里应该得到同一个“通过/失败”判定。
- **false-pass**：补丁其实不能在干净环境工作，却被轻量环境判成通过。
- **false-fail**：补丁本来是对的，却被轻量环境判成失败。

官方工作本身解决的是“容器太重，怎样更便宜地训练软件 Agent”；本项目问的是下一层问题：“变轻以后，裁判判得还一样吗？”[论文](https://arxiv.org/abs/2602.11210)｜[仓库](https://github.com/lblankl/SWE-MiniSandbox)

## 二、一个十分钟就能懂的假想案例

下面是**教学用的受控例子，不是已经在官方仓库测出的缺陷**。

某个小项目有函数：

```python
def parse_date(text):
    # bug: 这里只能处理 YYYY-MM-DD，不能处理题目要求的英文月份
    ...
```

Agent 提交了一个补丁：

```python
from dateutil import parser

def parse_date(text):
    return parser.parse(text).date()
```

逻辑上它能处理 `"25 Aug 2026"`，但 Agent 忘了把 `python-dateutil` 写进项目依赖。

### 环境 A：被复用过的缓存环境

```text
$ python -m pytest
3 passed
```

因为前一个任务或预构建环境里碰巧已经装过 `python-dateutil`，测试通过，裁判给 1 分。

### 环境 B：从声明依赖重新建立的干净容器

```text
$ python -m pytest
ImportError: No module named 'dateutil'
```

同一个补丁得 0 分。真实部署时也可能失败，所以 A 是一次 `false-pass`。

这里的科研问题不是“帮项目安装 dateutil”，而是：

1. 这种判定差异是否真实存在、出现多频繁；
2. 它由依赖污染、临时文件、环境变量、网络、超时还是测试收集造成；
3. 怎样用很小成本发现“这个任务不能安全复用环境”；
4. 修复后是否保持 MiniSandbox 的速度/磁盘优势。

## 三、真正实验不是只演示一个例子

### 1. 两条对照路径

对同一个仓库、同一个提交、同一个 Agent 补丁、同一条测试命令：

```text
路径 A：从声明依赖建立干净 Docker 环境 → 运行测试 → 保存完整日志
路径 B：MiniSandbox/预缓存环境           → 运行测试 → 保存完整日志
```

除了环境创建方式，其他条件尽量相同。若两边不一致，再重复运行和逐项消融，不能看一次日志就下结论。

### 2. 第一版故障矩阵

| 故障族 | 人为控制变量 | 要看什么 |
|---|---|---|
| 隐式依赖 | 只在缓存环境预装一个未声明包 | 是否 false-pass |
| 依赖版本 | A/B 使用不同小版本，再锁回相同版本 | 差异是否来自版本而非 sandbox |
| 残留文件 | 前一任务留下缓存/生成文件 | 新任务是否读取了旧状态 |
| 环境变量 | 只在一边设置开关 | 测试路径是否被静默改变 |
| 网络 | 允许/禁止测试时下载 | 离线时是否变成跳过或超时 |
| 测试收集 | 制造 0 tests、部分 skip、超时 | harness 是否把异常情况误判为通过/普通失败 |

官方实现已经有独立工作目录、`venv`、只读/读写挂载和 per-sandbox `/tmp` 等设计；研究不能假装这些措施不存在。故障注入要检验它们是否覆盖所有实际状态，而不是故意使用明显错误配置。[核心部署文件](https://github.com/lblankl/SWE-MiniSandbox/blob/main/sandboxdev/swesandbox/sandbox_deployment.py)

### 3. 最小结果表

下表数字仍是**格式示例，不是实验结果**：

| 故障族 | 实例数 | 判定一致 | false-pass | false-fail | 重复3次一致 | MiniSandbox准备时间节约 |
|---|---:|---:|---:|---:|---:|---:|
| 隐式依赖 | 20 | 17 | 3 | 0 | 20 | 71% |
| 残留文件 | 20 | 19 | 1 | 0 | 19 | 69% |
| 网络/超时 | 20 | 18 | 1 | 1 | 17 | 70% |

一张这样的表比“我跑通了 ICML 项目”有价值，因为它回答了原论文之外的可靠性问题。

## 四、怎样从 L2 走到 L3

### L1：缩小复现

证明官方最小任务在 MiniSandbox 和容器中都能跑；重现一项准备时间/磁盘趋势。

### L2：系统诊断

完成差分 harness、故障分类、重复性检查，得到可复验的 false-pass/false-fail 边界。即使所有受控条件都没有差异，也要能说明覆盖范围和统计上限。

### L3：证据驱动的小修复

只有某一类错误稳定出现，才做一个对应修复。例如：

- 执行前生成环境指纹，发现依赖/变量不一致就回退干净环境；
- 检查测试收集数、退出码和关键日志，避免“0 tests”被误读；
- 对可能读取的状态做最小白名单，而不是粗暴复制整个容器。

对照必须同时回答：错判减少多少、准备时间增加多少、磁盘增加多少、是否引入新 false-fail。

## 五、为什么这不是“难度 5 的小工具”

难度不来自代码行数，而来自四件事：

1. 你必须定义什么叫“两个环境等价”；
2. 你必须排除依赖版本、随机性和测试本身的混淆；
3. 你必须让故障可重复，而不是收集偶然报错；
4. 修复要减少错误，同时保留原系统的性能收益。

这类问题直接决定软件 Agent 的训练 reward 和 benchmark 可信度。它不像训练大模型那样显眼，但与 ICML 2026 原工作高度直接，也沉淀软件测试、AI 系统、Agent 评测和 Linux 能力。

## 六、答辩时三分钟怎么讲

> 软件 Agent 用测试结果作为训练奖励，但现有容器方案很重。SWE-MiniSandbox 通过轻量隔离和预缓存降低了磁盘与准备开销。我关注一个没有被性能数字直接回答的问题：环境复用是否保持测试判定语义。我建立了干净容器与 MiniSandbox 的差分 harness，对隐式依赖、状态残留、网络和测试收集做受控注入，测量 false-pass/false-fail 和重复性。实验发现……；因此我设计了……修复，在保留……性能收益时，把错判从……降到……。失败边界是……。

如果到项目结束仍只能说“我把官方仓库跑起来了”，就没有达到目标。

## 七、钱能与不能解决什么

- 能解决：租正确 Linux 主机、CPU/内存/磁盘、并行跑更多实例。
- 不能解决：差分条件是否公平、测试判定是否真的等价、错误分类是否成立、许可是否允许直接再分发原代码。

## 八、读完后的自检

不查资料，尝试回答：

1. 为什么测试通过也可能是假通过？
2. 为什么不能只比较一次 MiniSandbox 和 Docker？
3. 如果没有发现错判，项目是否一定失败？
4. 修复错判时为什么还要测时间和磁盘？
5. 你是否愿意连续数周看 Linux、依赖和测试日志？

前四题答不出只说明材料还需讲解；第五题才是用户对工作形态的有效偏好。

