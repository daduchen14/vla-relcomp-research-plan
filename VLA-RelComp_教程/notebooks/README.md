# Notebooks 说明

本版把免费轻量实验实现为 `scripts/` 下的纯 Python 小程序，原因是它们更容易在无 Jupyter、无 PyTorch 的零基础环境复现，也便于保存命令和退出码。Notebook 不是验收硬依赖。

正式学习者若偏好 notebook，可逐段复制以下脚本并保持同名输出：`action_chunk_demo.py`、`stage_probe_demo.py`、`analyze_registry.py`。不要在 notebook 内嵌 token、下载模型或把输出单元格当作最新实测证据；每次运行另存日期、环境和输入文件哈希。
