# 知识路线与答辩自检

## 一、必须掌握的公共底座

### 第4天前

- Python 虚拟环境、包、路径和异常；
- NumPy 数组与 PyTorch tensor；
- CPU/GPU、显存、batch、inference mode；
- RGB 图像、机器人状态、语言指令、连续动作；
- episode、step、reward 与 success 的区别。

### 第8天前

- Transformer 的 token、embedding、attention 和自回归；
- VLM 如何把图像与文字变成共同表示；
- VLA 如何把表示映射成7维动作或动作块；
- 行为克隆/SFT 的训练样本是什么；
- L0/L1/L2、训练分布、OOD 和组合泛化；
- baseline、seed、初始状态和置信区间。

### 第14天前

- CBDDL 中 init、object of interest、goal predicate；
- 仿真坐标、位姿、夹爪接触、抬升和终态；
- 最小反事实、控制变量、oracle、因果归因；
- 数据泄漏、post hoc 假设、对照和消融；
- 为什么负结果也可能是可靠研究结果。

## 二、暂时不要求

- RLHF、PPO、GRPO 的完整推导；
- 从零训练 Transformer 或 VLA；
- ROS、真机标定、动力学控制和机械臂硬件；
- 多机分布式训练；
- 整个机器人学课程；
- 为所有热门模型背架构细节。

只有项目证据要求时才补这些，不能因为名词热门就提前展开。

## 三、四个核心类比

1. VLM：看到图片后回答问题；
2. VLA：看到图片、状态和指令后连续输出动作；
3. 组合泛化：学过“番茄”“碗”“在上面”，但没见过这三者当前组合，仍能正确行动；
4. oracle：临时把某个环节的正确答案告诉系统，看整体是否恢复，用来定位瓶颈，不是最终作弊方法。

## 四、每日口述模板

每天不用背术语，只回答：

1. 今天的问题是什么？
2. 输入、模型、动作、环境反馈怎样流动？
3. 我只改变了哪个变量？
4. 结果来自哪些原始证据？
5. 还有哪两种替代解释？
6. 明天的实验如何区分它们？

连续两天答不出，停止扩大实验，先补理解。

## 五、第14天必须能回答

- 为什么不用 RoboTwin 直接做 Demo？
- 为什么不是复现 VLA-Arena 的平均分？
- 为什么先比较 SmolVLA 和 OpenVLA？
- L0 成功太低为什么会破坏诊断？
- success、target lifted 和 relation satisfied 有什么区别？
- 哪些变量必须固定才能叫匹配反事实？
- oracle 恢复能证明什么，不能证明什么？
- 为什么 L1/L2 不能参与训练和调参？
- 如果最小修复不涨点，项目还剩下什么贡献？
- 什么证据会让我们放弃主项目并切换 Fine-R1？

## 六、三分钟答辩骨架

第一分钟：VLA-Arena 显示模型从 L0 到 L1/L2 严重下降，但总体成功率无法告诉我们究竟是关系、对象还是控制失败。

第二分钟：我在同一套件上构造匹配反事实，并记录目标接触、抬升、参照接近和终态四段事件；再用语言与视觉 oracle 测量哪种信息能恢复失败。

第三分钟：根据诊断，只做一个最小修复，并用 L0 保持、L1/L2 泛化、多个种子和消融验证；如果修复无效，也明确报告瓶颈、失败边界和不能推出的结论。
