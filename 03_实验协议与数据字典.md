# 实验协议与数据字典

## 一、研究问题

VLA 在训练组合上成功、在未见空间关系组合上失败时，主要瓶颈究竟在哪里？能否通过只使用 L0 的最小修复提高 L1/L2，同时保持 L0？

不允许把“模型真正理解”作为不可证伪结论。本文只测行为稳定性、状态事件和受控干预后的恢复。

## 二、四段可观测链

| 阶段 | 操作性定义 | 可以说什么 | 不能直接说什么 |
|---|---|---|---|
| 目标接近/接触 | 末端或夹爪首先稳定接近、接触哪个可动物体 | 行为是否指向正确目标 | 模型内部一定识别了目标 |
| 抓取/抬升 | 正确目标与夹爪接触，并离开初始支撑面达到阈值 | 抓取阶段是否完成 | 模型具有抽象抓取概念 |
| 搬运到参照区域 | 目标与正确参照物/目标区域距离持续下降 | 轨迹是否朝正确参照移动 | 模型已建立语言 grounding |
| 终态满足 | CBDDL goal predicate 返回成功 | 任务是否完成 | 前三阶段都一定正确 |

语言解析和视觉 grounding 是潜变量，只能通过匹配反事实、视觉提示和语言 oracle 的恢复差异进行推断。

## 三、实验阶段

### E0：基础设施对照

- random 或无模型策略验证仿真、初始状态、视频和成功判定；
- 同一配置重复运行，确保差异来自明确 seed/init state；
- 任何环境异常不得记为模型失败。

### E1：官方模型基线

- SmolVLA 和 OpenVLA 分别做最小 pilot；
- 主诊断模型由 L0 成功样本和执行稳定性决定；
- 报告任务级而非只报宏平均，防止一个任务掩盖其他任务。

### E2：匹配反事实

每一对实验尽量固定：

- 场景资产与相机；
- 初始状态和随机种子；
- 目标与参照物集合；
- 模型、checkpoint 和推理设置。

每次只改变一个有效因素：

- 空间关系；
- 目标/参照物组合；
- 指令表达，但任务语义不变。

若改变指令后环境中的正确目标没有同步改变，该 pair 无效。所有新任务先由 CBDDL goal 和人工回放确认可达。

### E3：诊断 oracle

优先只做两种：

1. 语言 oracle：把自然语言改写成统一的“目标—起始关系—动作—终止关系—参照物”格式；
2. 视觉对象提示 oracle：利用仿真真值给目标和参照物增加明确但可撤销的视觉提示。

两者都保留无干预对照，并注明使用了仿真特权信息。oracle 结果只用于回答“哪个环节限制最大”，不作为最终部署方法或正式涨点。

### E4：最小修复

只有 E2/E3 给出稳定证据后才选择：

- 语言瓶颈：关系规范化或对比式 L0 SFT；
- 对象选择瓶颈：只使用可从公开视觉输入得到的对象/关系辅助，不使用测试真值；
- 控制瓶颈：先接受诊断结果，除非存在70天内可解释的轻量控制修复；
- 多环节同时失败：不堆多个模块，优先完成高质量负结果与边界。

## 四、核心指标

### 基础指标

- 每任务、每 level、每 seed 的成功数和总 episode；
- Wilson 二项置信区间；
- episode 墙钟时间、峰值显存、异常率；
- L0 保持率，以及 L1/L2 相对和绝对变化。

### 阶段指标

- target_contact_rate；
- target_lift_rate；
- reference_approach_rate；
- goal_satisfaction_rate；
- 从前一阶段进入下一阶段的条件成功率。

### 配对与干预指标

- pair_both_success：一对任务是否均正确；
- pair_asymmetry：只在一个关系条件下成功的比例；
- intervention_recovery：原失败 episode 经干预后变成功的比例；
- intervention_damage：原成功 episode 经干预后变失败的比例；
- 对匹配二元结果使用 McNemar 或精确检验；样本小时同时报告原始计数和置信区间，不只报 p 值。

## 五、防止数据泄漏

- L1/L2 不参与训练样本选择、超参数搜索和 checkpoint 早停；
- 可以用极小、预先登记的 L1/L2 pilot 验证研究切口，但一旦用于设计方法，就必须另留最终测试初始状态；
- oracle 使用的真值字段不得进入最终修复的推理输入；
- 所有自定义任务先登记再运行，不根据模型输出临时改目标；
- 配置、代码提交、模型 revision、数据 revision、seed 和 init state 必须能回溯；
- 看过测试结果后新增的假设必须标为 post hoc，不冒充预注册假设。

## 六、episode 数据字段

| 字段 | 含义 |
|---|---|
| run_id / episode_id | 全局唯一标识 |
| timestamp | 启动时间 |
| repo_commit / code_commit | 官方与本项目代码版本 |
| model_id / model_revision | 模型与精确版本 |
| suite / level / task_id | 套件、难度和任务 |
| seed / init_state_index | 随机性与初始状态 |
| instruction_original / instruction_variant | 原指令和干预指令 |
| intervention | none、language_oracle、visual_oracle 或具体修复 |
| target_object / reference_object / relation | 从 CBDDL 登记的任务结构 |
| target_contacted / target_lifted | 前两阶段事件 |
| reference_approached / relation_satisfied | 后两阶段事件 |
| success / steps / wall_seconds | 结果与成本 |
| peak_vram_mb | 资源 |
| video_path / log_path / result_path | 原始证据位置 |
| exception / notes | 异常与人工备注 |

字段允许先为空，不允许用猜测补齐。

## 七、项目完成标准

最低合格：

- 官方基线可重复；
- 至少一个模型有完整任务级结果；
- 阶段事件和失败分类经过人工抽查；
- 至少一组匹配反事实和一种 oracle 干预；
- 无论正负，都有对照、限制和可复现仓库。

方法增量合格：

- 修复由诊断证据触发；
- L1/L2 有稳定收益；
- L0 未明显退化；
- 至少两到三个种子和必要消融；
- 与已有工作差异可准确说明。
