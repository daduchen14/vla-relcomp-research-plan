# 第四轮：五项新颖性与撞题审计 V1

> 快照：2026-08-25。本轮回答的不是“原论文强不强”，而是“围绕它设计的 70 天项目是否还有自己的问题”。检索范围以 2024—2026 年正式论文、官方项目页和官方仓库为主。尚未运行仓库，因此可执行性仍是书面判断。

## 一、结论先行

| 候选 | 原先设想 | 撞题结果 | 本轮处理 |
|---|---|---|---|
| SWE-MiniSandbox | 缓存污染与受约束复用 | 相邻工作很多，但“轻量环境与干净容器是否给出同一测试判定”仍有明确空位 | **保留，改写为环境语义一致性审计** |
| NavSpace | 动作状态账本、冲突时重观察 | CorrectNav、ESI-Bench、AdaNav 等已覆盖自纠、主动观察、感知—动作闭环 | **保留，但不再造导航模块；改写为评测协议与能力归因审计** |
| MagCache | 在线误差估计与动态缓存 | MagCache 本身已有累计误差和自适应缓存；ERTACache、ACID 等继续占据该机制 | **降为备用；只保留分布外与尾部失效审计** |
| Fine-R1 | 视觉证据一致性 reward、反事实扰动 | 视觉思维忠实性、FG-BMK、SpeciaRL 等与原切口高度重合 | **降级；可做学习型审计，不进入前三主方案** |
| PQCache | 层/查询/显存自适应检索预算 | 动态 KV 预算已拥挤，ParisKV 又直接解决漂移并报告对 PQCache 的大幅速度优势 | **淘汰为最终项目，保留为系统学习材料** |

本轮最重要的修正是：**默认路线不再是“给顶会论文再加一个模块”，而是“缩小复现→找出可重复的失效边界→做因果/差分诊断→只在证据支持时做轻量修复”。**这仍是独立研究型项目，因为问题定义、实验协议、因果证据和结论属于用户；它不是只跑官方 Demo。

## 二、SWE-MiniSandbox：保留

### 原论文和邻近工作

SWE-MiniSandbox 是 ICML 2026 工作，使用 Linux namespace、`chroot`、bind mount 和预缓存环境，目标是在不依赖逐任务容器的情况下训练软件工程 Agent；论文报告磁盘约为容器方案的 5%，环境准备时间约为 25%。[论文](https://arxiv.org/abs/2602.11210)｜[ICML 2026 列表](https://icml.cc/Downloads/2026)｜[官方仓库](https://github.com/lblankl/SWE-MiniSandbox)

相邻方向已经在研究测试式 benchmark 的虚高、环境合成和未污染任务，例如 ICML 2026 的 SWE-ABS、ResearchEnvBench。这说明“执行评测是否可信”是热门真问题，但当前检索没有发现一项与下述差分问题完全同构的公开工作。[ResearchEnvBench](https://arxiv.org/abs/2603.06739)

### 淘汰旧切口，冻结新切口

旧切口“更聪明的缓存/淘汰算法”容易退化成系统调度作业，也很难证明与 Agent 质量直接相关，因此取消。

新切口暂定为：

> **轻量 Agent 沙箱是否保持 benchmark 语义？——MiniSandbox 与干净容器之间的差分验证。**

具体不是攻击安全系统，而是构造可控、无恶意的环境变化：

1. 依赖版本变化与包安装副作用；
2. 前一任务留下的文件、缓存和环境变量；
3. 网络不可用或下载失败；
4. 测试被跳过、超时或错误收集；
5. 同一补丁在干净容器与预缓存环境中的通过/失败差异。

核心指标是 `false-pass`、`false-fail`、两环境判定不一致率、重复运行稳定性，以及准备时间/磁盘节约。L3 修复只能针对实际发现的一个主要失效源，例如状态指纹、污染检测或回退到干净环境；没有信号就不强行发明模块。

### 为什么它仍算 AI 项目

它研究的是软件 Agent 的执行反馈和 RL reward 是否可信，而不是普通网站后端。Agent 是否“学会修代码”，最终由测试环境给出的奖励定义；环境错误会把错误补丁奖励为正确，或反过来惩罚正确补丁。长期资产是 Linux 隔离、软件测试、实验设计、Agent 评测与系统测量。

### 本轮判断

- 新颖性：中高，需在正式开题前再做一次标题/摘要级检索。
- 70 天：高概率形成 L2，出现差分信号后有合理 L3 路线。
- 最大非算力风险：仓库根目录无 LICENSE；正式公开项目应优先写独立差分 harness，避免直接分发其源码，并向作者确认许可。

## 三、NavSpace：保留但换问题

### 原论文和邻近工作

NavSpace 是 ICRA 2026 的空间智能导航 benchmark，包含六类任务和 1,228 个轨迹—指令对，统一评测 API 模型、SNav 和 StreamVLN；官方结果显示开放 MLLM 平均成功率很低。[论文](https://arxiv.org/abs/2510.08173)｜[项目页](https://navspace.github.io/)｜[官方仓库](https://github.com/TidalHarley/NavSpace)

原先的“动作状态账本＋感知冲突时重观察”不再成立为优先创新：同组 CorrectNav 已用错误轨迹和自纠数据做后训练；ESI-Bench 直接研究主动感知—动作闭环，并指出 action blindness；AdaNav 等也在做不确定性驱动的自适应推理。[CorrectNav](https://ojs.aaai.org/index.php/AAAI/article/view/38942)｜[ESI-Bench](https://arxiv.org/abs/2605.18746)

### 新切口

> **NavSpace 的低成功率中，多少来自空间能力，多少来自输出协议、观察采样和动作接口？——协议归一化的能力归因审计。**

在固定模型和固定小子集上，分开测量：

1. 静态帧上能否答对空间问题；
2. 把相同判断转成合法动作时是否失败；
3. 自由文本、JSON schema、受约束动作 token 三种输出接口造成的差异；
4. 固定帧数、均匀采样、关键转向帧对结果的影响；
5. `stop`、非法动作和解析失败对 SR 的贡献；
6. 给定一次 oracle 感知或 oracle 动作后，后续失败是否仍存在。

研究结果应输出一棵可复验的失败分解树，而不是只展示“加提示词后涨了几点”。若一个协议修复在多个模型/任务上稳定减少非能力型错误，才进入 L3；否则 L2 的负结果本身也是可信结论。

### 与现有工作的边界

NavSpace 已经指出感知—动作不一致，但没有因此自动等价于“对接口因素做受控归因”。CorrectNav 和 ESI-Bench 更偏模型训练或新 benchmark；本切口冻结模型，研究评测结论是否被接口与采样混淆。它与现有工作相邻但不重复，正式开题时仍需补检 2026 年下半年论文。

这不是纯假设：当前官方 `common.py` 的注释明确记录过一个现已修复的 RGB/BGR 通道交换问题，旧实现会向视觉编码器输入红蓝互换的图像；评测 prompt、通用 parser 和不同路线支持的动作集合也并非完全同一层定义。已修 bug 不能拿来当用户贡献，但证明实现协议确实可能混淆能力结论。[官方评测公共代码](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/common.py)

### 本轮判断

- 新颖性：中，研究问题清楚，但需靠严谨对照而非新模块取胜。
- 70 天：API 路线和固定子集可控；不训练完整 7B SNav，不用真机。
- 最大非算力风险：HM3D 需申请；仓库根目录无 LICENSE；Habitat 环境和结果归因比模型调用更耗认知。

## 四、MagCache：降为备用

MagCache 是 NeurIPS 2025 Main 工作，提出统一 magnitude law 和 training-free 动态缓存，在多个生成模型上报告约 2.10×—2.68× 加速。[NeurIPS 论文页](https://proceedings.neurips.cc/paper_files/paper/2025/hash/311207bb626e36a8f1d3eb92aa67af22-Abstract-Conference.html)｜[官方仓库](https://github.com/Zehong-Ma/MagCache)

旧 L3“定期真实 forward、在线误差估计、动态阈值”与原论文及后续工作重合：ERTACache 分解和校正缓存误差，ACID 又按关键时间步动态切换阈值。因此不能再包装为自主创新。[ERTACache](https://arxiv.org/abs/2508.21091)｜[ACID](https://arxiv.org/abs/2607.12358)

仍可保留的窄问题是：

> **一条样本校准出的 magnitude law 在运动强度、文字渲染、镜头切换和模型版本变化下是否保持？其平均指标是否掩盖少量严重失败？**

只做固定 1.3B 路线、提示词分层、尾部失败率和速度—质量 Pareto 前沿；只有检测到稳定的分布漂移，才尝试一个简单的分层校准。该项目可视化强、Apache-2.0 清楚，但方法赛道过于拥挤，知识抗周期也弱于前两项，因此作为执行备用而非主线。

## 五、Fine-R1：降级

Fine-R1 是 ICLR 2026 工作，以 CoT SFT 和 TAPO 让多模态大模型做细粒度识别，官方提供 3B/7B 路线、Stage-1 checkpoint、Stage-2 数据和训练脚本。[ICLR 论文页](https://proceedings.iclr.cc/paper_files/paper/2026/hash/6f6dd92b03ff9be7468a6104611c9187-Abstract-Conference.html)｜[官方仓库](https://github.com/PKU-ICST-MIPL/FineR1_ICLR2026)

原设想“视觉证据一致性、遮挡/背景交换、证据 reward”已经高度拥挤：

- Fine-R1 自己已分析性能改善更像“调用已有子类知识”，而非明显增强视觉线索提取；
- 视觉思维忠实性工作已用视觉/文本干预测 CoT 是否真正依赖图像；
- 2026 年 FG-BMK（同样含彭宇新）已做细粒度大模型的诊断，并包含视觉和语言扰动；
- SpeciaRL 等又继续推进细粒度开放世界 RL。

[视觉思维忠实性](https://arxiv.org/abs/2510.23482)｜[FG-BMK](https://arxiv.org/abs/2606.19053)

因此 Fine-R1 仍适合作为学习 SFT/RL/多模态评测的样本，但当前没有足够干净的 L3 切口进入最终前三。若未来恢复，只能先提出不同于“证据/扰动/通用 reward”的问题，并且单独解决导师团队风险；不能因它最接近热门词就降低新颖性标准。

## 六、PQCache：从最终项目淘汰

PQCache 是 SIGMOD 2025 工作，用 product quantization 和 MIPS 检索长上下文中重要 KV；其系统问题和知识迁移性都很强。[论文](https://arxiv.org/abs/2407.12820)｜[官方仓库](https://github.com/HugoZHL/PQCache)

但原 L3“按层/查询/显存动态分配检索预算”已经是拥挤套路，更关键的是 2026 年 ParisKV 直接把 drift robustness 和 GPU-native retrieval 作为贡献，并报告在百万 token 场景中相对 PQCache 的显著延迟优势。[ParisKV](https://arxiv.org/abs/2602.07721)

继续围绕 PQCache 做漂移或自适应预算，容易变成复现新论文为何超过旧论文，而不是自己的研究问题；要追上当前水平又需要较高 CUDA/系统认知。因此本轮把它从 70 天最终项目淘汰，但保留为学习 Transformer、KV cache、向量检索、量化和 CPU/GPU 内存层次的高质量材料。

## 七、这一轮对方法论的永久修正

1. 论文越新，不代表给它加一个模块就越新；作者的同期/后续工作可能已经吃掉最自然的增量。
2. “审计/诊断”不天然低档次。强度来自因果对照、覆盖面、复验性和是否发现真实边界，而不是代码行数。
3. L3 不预先承诺一个改进模块。先用一周左右找稳定信号；没有信号就退出，避免为了项目故事伪造问题。
4. 新颖性是动态变量。正式开题、完成基线、准备最终报告前各补检一次，而不是只在选题时查一次。
