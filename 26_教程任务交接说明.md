# VLA-RelComp 教程任务交接说明

> 交接版本：H1，2026-08-25。本文只保留为“教程制作与验证”的历史交接记录，不再是当前执行入口。当前唯一首次入口是 `VLA-RelComp_教程/h2_preflight/fresh_clone_quickstart.md`，随后进入 `VLA-RelComp_教程/README.md`。本文不取代正式项目决策 D1；凡涉及项目方向、研究问题和推翻条件，仍以 `25_正式项目决策D1_VLA-RelComp.md` 为最高依据。

## 一、接手结论

选题阶段已经结束。新任务不得重新从“人工智能有哪些方向”开始调查，也不得重新排列此前的候选项目。

- 正式主项目：**VLA-RelComp——轻量/开源 VLA 空间关系组合泛化的因果失效分解与最小修复**；
- 方向：具身智能中的 VLA 组合泛化、视觉—语言 grounding 与可靠性；
- 官方锚点：[VLA-Arena（ICML 2026）](https://vla-arena.github.io/)；
- 唯一目标套件：`PrepositionCombinations`，当前代码注册名为 `extrapolation_preposition_combinations`；
- 备用路线：Fine-R1-3B 细粒度视觉证据充分性诊断；
- 当前状态：只完成选题、D1 和开工包。尚未租用 GPU、下载模型、运行 VLA-Arena、训练或得到实验结果；
- 当前任务：把已有的14天概要手册升级成面向零科研基础者的实操课程，并完成不产生付费的轻量验证；
- 当前任务不是：立刻完成70天研究、租GPU跑完整基线或重新寻找方向。

只有 D1 规定的硬条件、重大新约束或与本项目高度重合的直接撞题证据，才允许建议推翻选题。普通安装困难、新名词、新论文或一时兴趣变化不能触发重选。

## 二、用户冻结画像

- 目标：初试后约70天全职投入，完成一个强组能识别价值、复试时本人可以讲透的研究型项目；
- 预计起点：考研数学、408、C语言，接触过 Python、Linux、Git，写过数据结构代码；
- 科研起点：没有正式科研、项目、竞赛和实习成果，需要从第一次完整闭环开始；
- 时间：正式项目期每天可投入8小时以上；
- 算力：愿意付费租用合适的 Linux/NVIDIA GPU，不应以当前笔记本能否运行倒推项目价值；
- 可用辅助：可以大量使用 Agent 解决资料、代码和排错问题；
- 优势：表达与答辩、从真实场景发现问题、沿因果链理解机制、愿意执行结构化方案；
- 风险：容易无限向底层深挖；可能在核心成品形成前追求全部理解；由 Agent 完成后本人无法解释；
- 学习偏好：先看到具体场景和可观察失败，再学习解决当前问题需要的知识；不适合先背大量抽象名词；
- 长期要求：项目应沉淀 PyTorch、Transformer、多模态、实验设计、评测和可靠性等可迁移能力，不把70天押在短命 API、提示词技巧或单一 benchmark 配方上；
- 兴趣口径：没有可靠科研体验时，“不知道”是有效信息。不得通过反复追问细分偏好制造伪偏好；明确反感可以否决工作形态。

教程应假设用户“听过部分术语但不会使用”，不得因为用户说听过 Transformer、SFT 或 VLA，就省略基础解释。

## 三、项目为什么这样设计

VLA-Arena 已经证明多个模型从 L0 到 L1/L2 的组合泛化显著下降。因此以下内容不能冒充本项目创新：

- 仅复跑 L0/L1/L2 并再次报告泛化差；
- 只展示注意力图、失败视频或平均成功率；
- 普通提示词改写、图像噪声或无机制解释的调参；
- 从头训练大型 VLA；
- 只完成可演示 Demo。

VLA-RelComp 的研究增量是行为级受控诊断：

1. 构造匹配的最小反事实对，尽量固定场景、物体、初始状态和随机种子，只改变一个空间关系或对象组合；
2. 利用仿真状态把成功链分解为目标接近/接触、抓取/抬升、搬运到参照区域、终态关系满足；
3. 使用语言 oracle 和视觉对象提示 oracle 定位限制环节；
4. 只有诊断产生稳定证据，才允许选择一个最小修复；
5. 修复训练只使用 L0，L1/L2 严格留作泛化测试；
6. 即使修复不涨点，只要诊断、对照、失败边界和复现证据可靠，仍可以形成合格的研究型项目。

科研表达不得宣称模型“真正理解”或“没有理解”。只能根据行为稳定性、状态事件、匹配反事实和受控干预结果进行有限推断。

## 四、历史任务当时的资料集

当时的交接依赖于仓库根目录的 `README.md`、本文件、`25_正式项目决策D1_VLA-RelComp.md`、`01_前14天逐日执行手册.md` 至 `04_知识路线与答辩自检.md`。现在它们仅用于追溯决策与教程生产要求；执行者不应从本节重建路径或版本，应回到 H2.5.2 fresh-clone 页。

## 五、当前教程任务的最终产物

保留原来的概要执行手册，不覆盖它。当时的新增目录建议为（现已实现）：

```text
VLA-RelComp_教程/
  README.md
  00_课程使用说明与学习地图.md
  day00_开工前诊断与工具使用/
  day01_.../
  ...
  day14_.../
  notebooks/
  scripts/
  assets/
  answer_keys/
  validation/
  progress_log.md
```

具体文件拆分可根据可维护性调整，但必须保证学习者能按 Day 0—Day 14 顺序执行，并能区分教程代码、上游代码、实验代码和个人作业。

### 每一天必须包含

1. 当日目标，以及它为什么服务于 VLA-RelComp；
2. 前置知识和开始前自检；
3. 面向零基础的知识讲义；
4. 一个具体场景或最小例子；
5. 精确到网页小节、论文页码、仓库文件/函数的必读材料；
6. 每项材料的“为什么看、看哪里、看多久、看完会什么、哪些暂时跳过”；
7. 可复制的操作步骤、命令或 notebook；
8. 正常预期输出，以及输出每部分意味着什么；
9. 知识点在 VLA-Arena/VLA-RelComp 真实代码和数据中的位置；
10. 常见错误、诊断顺序、备用路线和最长止损时间；
11. 正常耗时区间、最低完成线、标准完成线和提前完成后的选做；
12. 当日必须保存的文件、截图、日志、图表或口述稿；
13. 自测题、参考答案和复试口述问题；
14. 对当天每一步标记 `实测`、`静态核验`、`估计—未运行` 或 `待用户执行`。

### 每个核心知识点必须采用同一教学闭环

```text
为什么现在需要它
  → 它是什么
  → 一个直观实例
  → 正式定义与必要原理
  → 在官方代码/数据中的真实表示
  → 亲手运行最小实验
  → 改变一个变量观察结果
  → 连接到VLA-RelComp研究问题
  → 自测和当日交付
```

例如，不能只要求“说清 observation、state、action、episode、success”。必须让学习者查看真实 episode，打印或观察对应字段，解释 state 与 observation 的差别、episode 如何结束、success 由谁判定，并用交付物和口述问题验收。

## 六、教材与外部资料的分工

教程正文必须能够独立讲清主干，不能把学习路线外包给一堆链接。资料优先级为：

1. 官方项目文档、论文、代码、模型卡和数据卡；
2. 与当前知识点直接相关的高质量官方或经典教学材料；
3. 为用户编写的通俗解释、图示和最小例子；
4. 大模型只作为补充解释、排错和自测助教，不作为唯一事实来源。

不得写“自行搜索 VLA”“自行学习 Transformer”或一次列出十个教程让用户自己选择。每个外部链接都必须有明确阅读范围和完成标准。可能随版本变化的命令、文件路径和硬件要求必须核验当前官方来源，并记录查询日期、仓库 commit 或模型 revision。

只教解决当前瓶颈所需的理论。第14天前不系统展开 RLHF、PPO、GRPO、ROS、真机标定、多机训练或整个机器人学课程，除非官方数据流使其成为必要前置。

## 七、时间设计原则

- 标准日按约8小时设计，但同时给出合理区间，不伪造分钟级精度；
- 建议把每天分成认知、跟做、独立改动、记录、脱离 Agent 口述五类任务；
- 每个任务块通常控制在25—90分钟；连续排错设置明确止损上限；
- 当天学习的概念原则上当天在代码、数据、日志或实验里看到；
- 不采用“先学十天完整理论，再开始项目”的瀑布式课程；
- 若真实执行耗时与估计不同，保留原估计，记录实测并说明修订原因；
- Day 0—3 应比后续天数写得更细，因为它们决定学习者能否正确进入项目。

## 八、验证协议

教程不能仅靠写作完成，必须分层验证：

### A. 静态验证

- URL 可访问且指向预期官方材料；
- 论文页码、仓库路径、函数和配置名真实存在；
- 命令与锁定版本相符；
- 术语定义、教程正文和实验协议不矛盾；
- 不把计划输出写成已经得到的实验结果。

### B. 免费轻量验证（当前持久目标内）

- 运行本地可安全执行的脚本、notebook、解析器和格式检查；
- 对不能在当前机器运行的 GPU/仿真步骤进行命令级和代码路径核验；
- 为关键步骤准备可验证的预期输出形态；
- 保存验证日志到教程目录；
- 不为追求“全部跑过”而把 macOS/M1 限制误判为项目不可行。

### C. 完整参考运行（下一阶段）

教程初版完成后，由 Agent 在经用户批准的正确 Linux/NVIDIA 环境按教程完整走一遍，记录真实时间、显存、下载量、输出、错误和修订。该阶段另设持久目标；未获得云平台、预算和付费授权前不得启动。

### D. 用户学习运行（最终阶段）

完成验证版以后，用户按教程亲自执行。Agent 可以辅助排错，但关键代码、实验设计、失败归因和答辩内容必须由用户复述或复写通过。

## 九、自主权限与暂停条件

### 当前教程任务可以自主执行

- 搜索和阅读公开网页、论文、官方文档及公开 GitHub 仓库；
- 使用子代理按“资料核验、代码映射、教学设计、质量审计”等独立工作包并行，但主任务负责统一口径和最终验证；
- 在当前工作目录创建和修改教程、脚本、notebook、模板和验证记录；
- 创建隔离的免费本地环境，运行安全、非破坏性的测试；
- 修复普通错误并重复验证；
- 维护简短进度日志；
- 在本地验收通过后，将本任务新增或修改的教程文件提交到既有私有仓库 `daduchen14/vla-relcomp-research-plan`，不得改为公开。

### 必须暂停并请求用户的情况

- 需要购买、租用或续费任何服务；
- 单项下载预计超过20GB，或累计存储明显超出当前约定；
- 需要用户账号登录、验证码、token、密钥或新权限；
- 可能删除/覆盖原始数据，公开仓库或产生其他难以撤销的外部影响；
- 官方资产发生重大变化，需要改变研究对象或实验定义；
- 发现直接工作已完成同一套“匹配反事实＋状态分段＋oracle恢复”的核心贡献，足以触发 D1 撞题审查；
- 必读文件缺失或存在无法合理消解的实质冲突。

普通不确定性、资料分散、链接变动、代码阅读困难和免费测试报错不构成暂停理由；应继续调查、记录证据并尝试有边界的替代方案。

## 十、质量纪律

- 事实、推断、教学类比、假想示例、实测结果必须明确分开；
- 不伪造运行结果、GPU耗时、显存、成功率、截图或日志；
- 不因教程需要流畅而隐藏依赖、失败分支和版本风险；
- 不直接修改上游官方代码而不保存补丁；
- 不上传密码、token、个人联系方式、模型权重、数据集、虚拟环境、缓存、大日志或视频；
- 不让 Agent 生成的大量文字掩盖核心学习目标；
- 每一课都要回答“删掉这一节，用户是否仍能完成当前实验”。若能，降为选读；
- 教程写作不是最终研究成果。复现是地基，最终仍需独立问题、受控改动、对照/消融、结论和失败边界。

## 十一、当前持久目标的验收标准

只有同时满足以下条件，教程制作与轻量验证阶段才算完成：

1. Day 0—Day 14 全部形成可顺序执行的教程；
2. Day 0—3 达到最细颗粒度，零科研基础者无需自行拼学习路线；
3. VLM、VLA、observation、state、action、action chunk、episode、success、Transformer、视觉编码、行为克隆、L0/L1/L2、OOD、组合泛化、baseline、seed、置信区间、CBDDL、反事实、oracle、数据泄漏等核心概念均有项目内教学闭环；
4. 每天都有时间预算、资料入口、动手任务、预期输出、止损分支、交付物、自测和答案；
5. 所有关键链接、代码路径、配置名和命令经过静态核验；
6. 免费且适合当前机器的步骤已实际运行，并留下日志；
7. GPU/仿真未运行部分明确标为未运行，不以估计冒充实测；
8. 完成一次“零基础学习者视角”和一次“研究协议一致性”审计；
9. 教程 README 能说明使用顺序、预计总时长、软硬件闸门和下一阶段；
10. 更新私有 GitHub 仓库，并提交完成报告：完成内容、验证证据、仍需GPU验证的项目、主要风险和完整参考运行建议。

## 十二、接手后的第一步

新任务读取上述文件后，应先完成一个不超过一页的内部接手核验，确认：

1. 最终研究问题是什么；
2. 哪些决策已经冻结；
3. 当前任务交付什么、不交付什么；
4. 当前可以自主执行什么；
5. 哪些情况才允许暂停。

若没有发现硬冲突，不等待用户逐项确认，立即建立 `VLA-RelComp_教程/progress_log.md`，列出 Day 0—14 的制作与验证队列，并持续工作至本阶段验收完成。
