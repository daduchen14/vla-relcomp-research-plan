# 五项候选：零基础知识与工程地图 V1

> 快照：2026-08-24。事实来自官方论文、正式会议页、官方仓库和文档；时间均为“公共主干学完以后”的额外学习估计，不是进度承诺。本轮没有安装仓库、下载模型或租用算力。

## 一、先看五项到底是什么

| 候选 | 一句话人话版 | 研究类型 | 北大锚点 | 当前公开状态 |
|---|---|---|---|---|
| SWE-MiniSandbox | 让代码 Agent 在互不污染的环境中改代码、跑测试并得到可靠奖励 | Agent 基础设施×软件系统 | 张辉帅、赵东岩 | 2026 arXiv，未确认正式 venue |
| PQCache | 长文本推理时先检索少量重要历史 token，减少完整注意力 | LLM 推理系统×数据库检索 | 崔斌团队；苗旭鹏合作生态需区分入职前后 | SIGMOD 2025 |
| NavSpace | 检查导航 Agent 是否真的理解楼层、距离、方向和空间结构 | 具身导航×多模态 Agent×评测 | 董豪近期成果 | ICRA 2026；代码、数据和 SNav 权重公开 |
| Fine-R1 | 用带思考过程的 SFT 和 RL，让多模态模型分清相似鸟种、车型等 | 多模态×后训练 | 彭宇新团队 | ICLR 2026；两阶段代码、数据和模型公开 |
| MagCache | 视频生成时复用相邻去噪步骤的近似结果，以少量画质损失换速度 | 生成模型×推理加速 | 张史梁；北大—华为合作成果 | NeurIPS 2025；多后端代码公开 |

RoboTwin 2.0 仍是有价值的外部教程/仿真平台，但其官方论文作者单位不含北大，不能作为董豪成果；本轮由 NavSpace 替换。

## 二、五项共享的知识

无论最终选哪项，前 10—14 天的公共主干不浪费：

1. Python、Linux、Git、Conda/venv、日志与配置管理；
2. PyTorch 的张量、前向、损失、反向与优化器；
3. Transformer：Q/K/V、Attention、Mask、残差、归一化、RoPE、训练与自回归推理；
4. 大模型工作流：预训练、SFT、LoRA、RL/偏好优化、推理与评测的区别；
5. 科研实验：基线、控制变量、消融、多 seed、显著性、错误分类和失败边界。

不是每项都要训练 Transformer。例如 SWE 可能不训练模型，MagCache 是 training-free；但理解模型在整个系统中的输入、输出和成本仍然需要公共主干。

---

## 三、SWE-MiniSandbox

### 1. 你实际在研究什么

代码 Agent 会打开一个真实软件仓库、修改文件、安装依赖并运行测试。训练时，测试结果往往直接变成奖励。如果两个任务共享了不该共享的文件或依赖，Agent 可能因为“环境污染”拿到错误奖励。

SWE-MiniSandbox 用 Linux namespace、`chroot`、bind mount 和预缓存的 Python 环境代替每题一个完整 Docker。论文报告其磁盘占用约为容器方案的 5%，环境准备时间约为 25%。[论文](https://arxiv.org/abs/2602.11210)；[官方仓库](https://github.com/lblankl/SWE-MiniSandbox)。

### 2. 输入、输出和因果链

```text
软件问题 + 初始仓库
  → 为 Agent 创建隔离工作区
  → Agent 修改代码并运行测试
  → 测试结果转成奖励
  → 清理/复用环境
```

核心问题不是“模型能不能写代码”，而是“系统给出的奖励是否真实、隔离是否可靠、批量运行是否高效”。

### 3. 额外要学什么

| 知识 | 学到什么程度 | 估计 |
|---|---|---:|
| Linux 进程、文件系统与权限 | 能解释 namespace、mount、chroot、进程树和清理 | 3—4 天 |
| Python venv/Conda 与依赖复用 | 能定位依赖污染、缓存失效和版本冲突 | 2—3 天 |
| SWE-bench/SWE-agent | 知道任务、patch、测试与奖励如何对应 | 2—3 天 |
| 系统测量 | 冷/热启动、磁盘、吞吐、P50/P95、正确性 | 2 天 |

额外总负担：约 8—12 天。Transformer 只需理解 Agent 模型在系统中的位置；如果不做 RL，不必掌握完整强化学习推导。

### 4. 工程真实形态

- 根仓库有 2,500 余文件，是因为打包了 SWE-agent、SWE-bench、SkyRL 等多个上游；不应试图全部读懂。
- 自身 `sandboxdev/` 约 39 个文件，主要核心集中在 `sandbox_deployment.py`，另有配置、环境映射和一个隔离测试。
- 官方完整 RL 路线很重：示例包含 8 卡 SFT、Ray、每节点 128 个 sandbox 等。本项目明确**不复制完整 RL 训练**。
- 官方要求 Linux、mount namespace、bind mount；快速 Docker 路线需要 privileged。仓库根目录目前未见明确 LICENSE，公开派生代码前需解决许可口径。

### 5. 14 天闸门

必须完成：

1. 在专用 Linux 主机跑通 3—5 个固定软件任务；
2. 同一任务在 Docker 与 MiniSandbox 中得到一致测试结果；
3. 记录冷/热启动、磁盘、执行时间和清理后残留；
4. 人为制造一次依赖/文件污染，并证明检测器能发现。

若 14 天仍无法解释一次奖励误判是如何产生的，淘汰；只测启动速度也淘汰。

### 6. 从复现到独立项目

- **L1**：重现 Docker 与 MiniSandbox 的小规模效率差异。
- **L2 保底**：建立环境污染和奖励误判分类，形成可重复的 correctness benchmark。
- **L3 候选**：在容量受限时设计“带正确性约束的环境复用/淘汰策略”，同时优化启动速度、磁盘和奖励一致性。

独立性来自：你定义污染类型、构造测试协议、提出复用策略并证明它没有换取错误奖励，不来自重新运行作者的 1,600 个任务。

### 7. 抗周期价值

长期资产很强：Linux、软件测试、隔离、Agent 评测、系统实验和错误归因。易折旧部分是 SkyRL/SWE-agent 的具体接口。

最大疑问：最终成果可能更像 AI 系统/软件工程，而不是传统“训练一个模型”；复试是否加分取决于目标老师是否理解并重视该交叉方向。

---

## 四、PQCache

### 1. 你实际在研究什么

LLM 生成每个新 token 时，通常要查看前面所有 token 的 Key/Value。上下文越长，KV cache 越吃显存。PQCache 把历史 Key 压成产品量化编码，先用近似检索找出可能重要的 token，再只取回这些 token 的真实 KV 做注意力。[SIGMOD 2025 接收页](https://sigmodconf.hosting.acm.org/2025/sigmod_papers.shtml)；[论文](https://arxiv.org/abs/2407.12820)；[官方仓库](https://github.com/HugoZHL/PQCache)。

### 2. 输入、输出和因果链

```text
长文本 + 当前查询 token
  → 将历史 Key 做 PQ 编码
  → 用 MIPS 近似找重要历史位置
  → 取回少量真实 KV
  → 计算注意力并生成下一个 token
```

研究目标是同时维持答案质量、低显存和低延迟，而不是单独追求压缩率。

### 3. 额外要学什么

| 知识 | 学到什么程度 | 估计 |
|---|---|---:|
| Transformer 推理与 KV cache | 能画出 prefill/decode、各张量形状和显存来源 | 3—4 天 |
| Product Quantization、MIPS | 能手算小例子，解释码本、子空间和召回误差 | 3—4 天 |
| CPU/GPU 内存与并行 | 能解释压缩、检索、数据搬运如何重叠 | 2—3 天 |
| LongBench 与性能实验 | 能同时测质量、P50/P95、吞吐、峰值显存 | 2—3 天 |

额外总负担：约 10—14 天。数学主要是线性代数、向量距离和近似检索，不需要证明新定理。

### 4. 工程真实形态

- 仓库约 128 个文件，核心集中在 `vq_method/retrieval_based/`：缓存管理、PQ 搜索、多核压缩和一个 GPU LFU 模块。
- 官方要求 CUDA 12.1/PyTorch 2.1.2、FlashAttention、Compute Capability ≥ 8.0、显存 ≥ 24GB；已注明 A800 与 RTX 4090 可运行。
- 默认压缩使用 48 个 CPU 核，但可降低；支持 Llama-3.1-8B 和 Mistral-7B。
- 仓库目前未见明确 LICENSE。实验可以先做，但公开修改版代码前需请求许可或把自己的实验/策略写成独立插件，不复制受限源码。

### 5. 14 天闸门

1. 固定一个 7B/8B 模型和 LongBench 一个任务；
2. 跑通完整注意力基线和 PQCache；
3. 至少测两档检索比例；
4. 同时报答案质量、端到端时延、峰值显存和 CPU 时间；
5. 定位至少一类“检索召回高但答案仍错误”或“压缩高但尾延迟恶化”的失败。

若只能运行脚本，不能解释某层某头的 Key 如何被编码、查找和取回，淘汰。

### 6. 从复现到独立项目

- **L1**：重现一个任务上的质量—延迟—显存曲线。
- **L2 保底**：系统分析层、上下文类型、检索比例和 CPU 线程对质量/尾延迟的影响。
- **L3 候选**：在固定平均取回预算下，根据层、查询不确定性或显存压力动态分配检索比例，并验证是否改善 Pareto 前沿。

不能把“多扫几个参数”包装成创新。L3 必须先提出为什么不同层/查询不应使用同一预算，再用消融证明。

### 7. 抗周期价值

五项中属于最高一档：Transformer、检索、量化、内存层次和性能测量都能迁往模型推理、数据库、推荐/搜索和系统工程。具体 Llama patch 与 FlashAttention 版本会折旧。

最大疑问：新 KV cache 方法更新很快；最终切口必须经过撞题检索，而且崔斌—苗旭鹏的当前北大指导与资源边界仍需单独核验。

---

## 五、NavSpace

### 1. 你实际在研究什么

一个导航 Agent 接收“上到二楼、经过走廊、停在卧室门口”这类指令，然后不断看第一视角图像、决定前进/转向/抬头/停止。NavSpace 专门检查 Agent 是否真正理解楼层、距离、角度、视角变化、空间关系和环境状态。

官方提供六类任务、1,228 条轨迹—指令对，评测过 22 种导航 Agent；论文指出现有模型常出现“嘴上感知对了，实际动作仍不一致”。[论文](https://arxiv.org/abs/2510.08173)；[官方仓库](https://github.com/TidalHarley/NavSpace)；董豪个人主页将其列为 ICRA 2026 成果。

### 2. 输入、输出和因果链

```text
语言指令 + 到当前为止的第一视角图像/历史动作
  → Agent 形成空间判断
  → 输出 forward/left/right/look-up/look-down/stop
  → Habitat 执行动作并返回新观察
  → 直到成功、超步数或错误停止
```

这比静态图片问答更接近真正 Agent：模型的每一步会改变下一步看到的世界，早期错误会累积。

### 3. 额外要学什么

| 知识 | 学到什么程度 | 估计 |
|---|---|---:|
| 坐标、姿态和离散动作 | 理解位置、朝向、相对方向、路径和停止条件 | 2—3 天 |
| Habitat-Sim/HM3D | 能加载场景、执行动作、复现实验轨迹 | 3—5 天 |
| 多模态 Agent 闭环 | 理解图像/历史/指令如何变成动作 | 3—4 天 |
| 导航评测 | 成功率、路径效率、停止错误、多 seed 和错误阶段 | 2—3 天 |
| 时序记忆/状态一致性 | 区分“看错、记错、推理错、动作映射错” | 2—3 天 |

额外总负担：约 12—18 天。比 RoboTwin 双臂操作少一整层运动控制和机械臂 policy 训练，但仍不是普通视觉问答。

### 4. 工程真实形态

- 官方仓库提供离线 smoke test、数据完整性检查、在线 API 评测、本地 SNav/StreamVLN 评测、标注界面和两阶段训练代码。
- 可以先走 API/已发布 SNav-7B 权重的评测路径，不必从零训练 7B 模型。
- 真正仿真需要 Linux、Habitat-Sim 0.2.x 和 HM3D 场景；HM3D 需填写申请，属于外部数据闸门。
- 完整 SNav 训练使用 LLaVA-Video-7B、DeepSpeed、16 帧输入和最长 32K 序列；不作为前两周必达项。
- 仓库当前未见明确 LICENSE；公开派生实现前需澄清。代码和数据在 2026-08 仍有更新。

### 5. 14 天闸门

1. 离线 smoke test 和 1,228 条数据完整性检查通过；
2. 获得 HM3D 权限并在一个场景渲染/执行动作；
3. 在一类任务、20—50 条轨迹上跑通一个 API 模型或发布的 SNav；
4. 把失败拆成感知、记忆、空间推理、动作映射和停止五类；
5. 证明至少一类错误可被稳定复现，而不是随机波动。

若 HM3D 权限未获得、Habitat 环境七天仍不能渲染，或错误无法稳定归类，退出。

### 6. 从复现到独立项目

- **L1**：重现一个模型在一个 NavSpace 子任务上的成功率。
- **L2 保底**：建立“感知正确但动作错误”等闭环失败分类，测不同历史长度、视觉帧数和模型调用预算。
- **L3 候选**：维护一个由历史动作更新的轻量自我位置/朝向状态；当模型感知与状态冲突时触发重观察或纠错。在固定视觉/token/API 预算下比较成功率、路径效率和错误类型。

这个切口不是“换提示词”。它明确改变 Agent 的状态表示和动作决策协议，但是否已有高度相似工作仍需做新颖性检索。

### 7. 抗周期价值

多模态观察、时序状态、工具/环境闭环、空间推理、仿真评测可以迁往机器人、游戏 Agent、自动驾驶仿真和通用多模态 Agent。Habitat 的具体版本会折旧。

最大疑问：它是导航而非操作，与用户原先看到的 RoboTwin 双臂 Demo 不同；但它的北大归属更清楚、工程链更短，也更容易形成真正可控的实验。

---

## 六、Fine-R1

### 1. 你实际在研究什么

通用多模态模型知道图片里是“鸟”，但可能分不清具体鸟种。Fine-R1 先用带视觉分析过程的数据做 CoT-SFT，再用 TAPO 强化学习，让模型同时学习同类内部变化和不同细类之间的区别。[ICLR 2026 正式页](https://proceedings.iclr.cc/paper_files/paper/2026/hash/6f6dd92b03ff9be7468a6104611c9187-Abstract-Conference.html)；[官方仓库](https://github.com/PKU-ICST-MIPL/FineR1_ICLR2026)。

### 2. 输入、输出和因果链

```text
图片 + “这是什么细分类别”
  → 多模态 Transformer 提取视觉与语言特征
  → SFT 学会“观察—候选—比较—结论”的输出格式
  → RL 根据答案、格式和增强样本关系给奖励
  → 输出推理过程和类别
```

SFT 是“给标准示范继续训练”；RL 是“模型先产生答案，再根据奖励更新”。真正难点不是记住缩写，而是判断奖励是否在教视觉辨别，还是让模型学会投机。

### 3. 额外要学什么

| 知识 | 学到什么程度 | 估计 |
|---|---|---:|
| VLM/MLLM | 理解视觉编码器、投影层、LLM 和多模态 token | 3—4 天 |
| SFT/CoT | 能读懂样本格式、loss mask、全参/LoRA 和训练日志 | 3—4 天 |
| RL 后训练 | 理解 rollout、policy、reward、KL、优势和 reward hacking | 4—6 天 |
| 细粒度视觉与评测 | 类内变化、类间差异、closed/open world、长尾和域外测试 | 3—4 天 |

额外总负担：约 14—21 天，是五项中认知和训练链最重的一档。

### 4. 工程真实形态

- 仓库不是一套小脚本，而是 LLaMA-Factory（SFT）和 TAPO/verl（RL）两套训练栈，再加评测代码。
- 官方 3B SFT 脚本使用 4 卡全参数 DeepSpeed ZeRO-3；3B TAPO 脚本同样固定 4 卡、最长 4K prompt+4K response、10 epochs。
- 官方已发布 Stage-1 checkpoint，因此可跳过第一阶段全训，从评测或缩小 Stage-2 开始。
- README 明确给出 MIT 许可口径；代码、Stage-2 数据、3B/7B 模型和评测已公开。
- 算力能解决四卡训练，但不能替代对 rollout、reward、数据泄漏和视觉证据一致性的理解。

### 5. 14 天闸门

1. 使用发布的 3B Stage-1 checkpoint 跑通一个数据集的 closed/open-world 评测子集；
2. 对原图、遮挡、背景替换或低画质样本建立固定测试集；
3. 读懂一条样本如何进入模型、如何产生 rollout、如何计算 reward；
4. 在 4 卡环境完成几十到几百步缩小 TAPO 训练并保存可比较 checkpoint；
5. 能说明性能变化来自训练还是评测噪声。

若 14 天后仍只能调用训练脚本，无法解释 reward 每一项的作用，淘汰。

### 6. 从复现到独立项目

- **L1**：重现发布 checkpoint 在一个子数据集上的结果。
- **L2 保底**：做遮挡、背景、图像质量和未见类别的失败地图，并检查 CoT 是否真的引用图像证据。
- **L3 候选**：在固定 rollout 预算下调整正/负增强样本与 reward 组合，使答案准确率、视觉证据一致性和域外泛化共同改善。

不能只“换一个数据集再训一次”。必须有一个明确假设，例如某类增强提高准确率却损害证据一致性，并设计对照验证。

### 7. 抗周期价值

VLM、SFT、RL、数据构造、reward 和评测高度可迁移；TAPO/verl 的具体参数和“R1”命名可能折旧。

最大疑问：四卡训练链会占用较多认知时间；此外，导师/团队匿名负面线索必须与项目学术价值分开做多源核查，不能由论文直接推导导师推荐。

---

## 七、MagCache

### 1. 你实际在研究什么

视频扩散/流模型通过很多时间步逐渐生成结果。相邻时间步的某些模块输出变化很小，MagCache 用 residual magnitude ratio 估计变化，在安全时复用前一步结果，避免重复计算。[NeurIPS 2025 正式页](https://proceedings.neurips.cc/paper_files/paper/2025/hash/311207bb626e36a8f1d3eb92aa67af22-Abstract-Conference.html)；[官方仓库](https://github.com/Zehong-Ma/MagCache)。

### 2. 输入、输出和因果链

```text
文字 prompt + 随机噪声
  → 多步去噪/流采样
  → 估计当前步与前一步的残差变化
  → 变化小则复用缓存，变化大则真实计算
  → 输出图像/视频
```

它本身不训练模型；研究重点是怎样决定“这一层、这一步能不能偷懒”，以及怎样证明画质没有在少数场景中严重崩坏。

### 3. 额外要学什么

| 知识 | 学到什么程度 | 估计 |
|---|---|---:|
| Diffusion/Flow | 理解时间步、噪声/速度预测、采样器和 DiT | 3—4 天 |
| 特征与残差缓存 | 能解释缓存对象、阈值、K 和 retention ratio | 2—3 天 |
| 生成质量评测 | PSNR/SSIM/LPIPS/VBench及其盲点 | 2—3 天 |
| GPU 性能测量 | warm-up、同步、端到端时间、显存和硬件差异 | 2—3 天 |

额外总负担：约 10—14 天。理论比 TransMLA 少，但生成评测比“看视频觉得不错”严格得多。

### 4. 工程真实形态

- 根仓库支持 Wan、HunyuanVideo、Open-Sora、FLUX、Qwen-Image 等多个后端；不要全读。
- 轻入口固定 Wan2.1 1.3B，核心实现主要是一份约 54KB 的 `magcache_generate.py`，但依赖完整 Wan 上游、模型权重和 CUDA。
- 官方单 A800 报告 Wan2.1 1.3B 480p 无缓存约 189 秒，MagCache 配置约 68—87 秒；这是作者硬件上的参考，不是本项目实测。
- 根仓库 Apache-2.0，适合公开复现实验和独立改动；上游 Wan 许可仍需单独遵守。

### 5. 14 天闸门

1. 固定 Wan2.1 1.3B、分辨率、prompt 和 seed；
2. 跑通无缓存、官方缓存两个基线；
3. 正确同步 GPU 后记录时间、峰值显存和图像/视频；
4. 建立至少 30—50 个覆盖文字、小物体、快速运动和复杂构图的 prompt 子集；
5. 找到平均指标正常但局部/尾部画质明显失败的可重复案例。

如果只能复现 2× 加速、找不到稳定失败结构，不能进入 L3。

### 6. 从复现到独立项目

- **L1**：重现一个轻后端上的速度—质量曲线。
- **L2 保底**：建立尾部 prompt/运动/文字失败分类，比较平均指标与最差样本指标。
- **L3 候选**：周期性执行真实 forward 估计累计缓存误差，在固定额外计算预算下动态调整缓存步数，比较质量、尾部失败率、时延和显存。

该候选最容易形成可视化成果，也最容易撞题。必须先做 2025—2026 缓存方法的新颖性检索；若闭环误差控制已有高度相同方案，就替换切口或取消第五席。

### 7. 抗周期价值

GPU 性能测量、缓存、质量—时延权衡和生成评测可迁移；特定 diffusion timestep 规律和 Wan 脚本更易折旧。抗周期性低于 PQCache/TransMLA，但工程闭环更短。

---

## 八、五项横向学习负担

以下均是在 10—14 天公共主干之外：

| 候选 | 额外学习 | 首个可信基线 | 主要代码掌握范围 | 最难用钱解决的部分 |
|---|---:|---:|---|---|
| SWE-MiniSandbox | 8—12 天 | 7—14 天 | 自身隔离核心＋少量上游接口 | Linux 语义与奖励正确性 |
| PQCache | 10—14 天 | 7—14 天 | PQ/缓存核心＋模型 attention patch | 质量、检索和性能的归因 |
| NavSpace | 12—18 天 | 7—14 天 | 一条评测路径＋状态/动作协议 | 动态失败分类与空间一致性 |
| Fine-R1 | 14—21 天 | 10—14 天 | 评测＋reward＋缩小训练入口 | RL/reward/数据因果链 |
| MagCache | 10—14 天 | 3—7 天 | Wan 轻后端的一份核心脚本 | 尾部画质评价与新颖性 |

“首个可信基线”不是最终项目。70 天中至少还要留下 25—30 天做自主改动、对照、消融和写作；因此第一条核心曲线不能拖到第 30 天以后。

## 九、当前认知结论

- **最像系统科研**：PQCache、SWE-MiniSandbox。
- **最像用户熟悉的热门 AI 词汇**：Fine-R1。
- **最像真正闭环 Agent/具身任务**：NavSpace。
- **最容易快速看到结果和做演示**：MagCache。
- **当前最抗周期**：PQCache；其次是 SWE、NavSpace；Fine-R1 的方法论可迁移但训练框架易变；MagCache 的项目技巧折旧最快。
- **当前最需要防止“Agent替你完成、你自己没掌握”**：Fine-R1 与 NavSpace。

