# 前14天逐日执行手册

> 原则：学习和工程并行。每天约8小时，建议分配为2小时概念学习、4小时实验、1小时记录、1小时脱离 Agent 的口述或复写。没有当天证据文件，就不算完成。

## 第1—4天：建立最小闭环

| 天 | 学习任务 | 实验任务 | 当天必须留下的证据 |
|---|---|---|---|
| D1 | 说清 VLM、VLA、观测、状态、动作、episode、success | 创建云实例；记录驱动、CUDA、GPU、磁盘；克隆候选提交；只做静态检查 | environment.md、仓库提交号、nvidia-smi输出、目录截图 |
| D2 | 理解张量、device、inference mode、7维动作和 action chunk | 同步 base 环境；用 random evaluator 或最小环境入口验证仿真、无头渲染和视频写出 | 一条可重复的环境 episode、日志、视频或明确故障归因 |
| D3 | 理解 Transformer、视觉编码器、语言指令与动作头的数据流 | 安装 SmolVLA 隔离环境；只下载官方 checkpoint；跑一个指定 L0 任务、1次 trial | 固定命令、配置副本、模型 revision、峰值显存、episode 视频 |
| D4 | 理解行为克隆和“模型运行成功不等于任务成功” | 同一配置重复3次；验证成功判定、日志、视频和初始状态选择；完成 Gate 1 | Gate 1 评审表；至少一次从输入到结果的口述录音或文字稿 |

### Gate 1：D4

必须同时满足：

- 正确的 Ubuntu/NVIDIA 环境中，官方模型单 episode 可重复启动；
- 日志能对应任务、level、seed、init state、动作步数和终态；
- 至少能保存一个视频；
- 用户能解释两张图像、机器人状态、语言指令、7维动作如何形成闭环。

未通过时只允许再用一个工作日处理明确故障。仍失败则检查官方 issue、提交差异和实例环境；不得悄悄换项目，也不得直接开始训练。

## 第5—8天：确定可分析的模型

| 天 | 学习任务 | 实验任务 | 当天必须留下的证据 |
|---|---|---|---|
| D5 | 理解 L0/L1/L2、分布外和组合泛化 | SmolVLA 跑5个 L0任务，每项5次；固定初始状态策略 | 25行 episode 索引、成功率、错误清单 |
| D6 | 理解基线、随机种子和置信区间 | SmolVLA 扩到 L1/L2；估计单 suite 完整评测墙钟时间 | 75行 pilot 或清楚记录为何提前停止 |
| D7 | 理解“轻量模型太弱也无法做诊断” | 在80GB GPU上跑 OpenVLA 官方 checkpoint 的单任务试验；不训练 | OpenVLA 单 episode、显存、时延、错误日志 |
| D8 | 理解模型选择不是模型排名 | 对 L0 能力足够的模型完成5任务 × 3等级 × 5次 pilot；完成 Gate 2 | 统一 CSV、按 level 的成功率、任务级分布和视频索引 |

### Gate 2：D8

选择“主诊断模型”，不选“最便宜模型”：

- 至少一个模型在 L0 有足够成功样本，暂定门槛为25个 L0 episode 中至少8次成功；
- 完整15任务多次评测按实测吞吐能在70天内完成；
- 失败不是大面积安装异常、NaN、模型未加载或渲染崩溃；
- 用户能区分模型失败、环境失败和评测脚本失败。

若 SmolVLA 低于门槛而 OpenVLA 通过，主诊断用 OpenVLA；后续训练仍可另选轻量模型。若所有官方模型都没有可分析的 L0 能力，触发 D1 推翻条件，转 Fine-R1 备用，而不是继续堆 GPU。

## 第9—14天：验证研究切口

| 天 | 学习任务 | 实验任务 | 当天必须留下的证据 |
|---|---|---|---|
| D9 | 阅读 CBDDL 中 init、obj_of_interest 和 goal | 逐项解析15个任务；生成任务—对象—关系—目标表；定位成功谓词 | task_manifest.csv、5项任务人工核对 |
| D10 | 理解可观测行为与不可直接声称的“内部理解” | 在 evaluator 的 episode 循环旁增加只读状态记录；不改变动作 | target contact、lift、transfer、goal 四类事件的样例轨迹 |
| D11 | 理解最小反事实和配对设计 | 设计2—3组匹配任务：固定场景/初始状态，改变一个有效关系或对象组合 | pair_manifest.csv、每对只改变什么、为何无标签泄漏 |
| D12 | 理解 oracle 只用于诊断 | 实现一个语言 oracle 和一个视觉对象提示 oracle 的最小原型；保存原始条件作对照 | 干预定义、代码差异、首轮结果 |
| D13 | 理解恢复率、配对检验和失败边界 | 固定至少两个种子复测；检查结果是否由单任务或单初始状态驱动 | 配对表、恢复率、置信区间、反例 |
| D14 | 用三分钟讲清问题—假设—实验—结果 | 完成 Gate 3；决定继续诊断、进入最小修复，或切换备用 | gate_review.md 与唯一书面结论 |

### Gate 3：D14

只有以下三种合法结论：

1. 通过：存在可重复阶段差异，至少一种诊断干预产生稳定恢复，进入 D15—35 扩大诊断；
2. 诊断通过、修复暂缓：失败分解可信，但尚不知道该改哪里；继续做 L2 诊断项目，不编造方法；
3. 退出：状态不可观测、配对任务无效、结果不可重复或直接撞题，切 Fine-R1。

“跑通了”“视频挺好看”“偶尔成功一次”都不构成通过。

## 14天之后

- D15—25：冻结主模型、任务、种子和指标，扩充可信基线与状态探针；
- D26—35：完成反事实和 oracle 因果诊断；
- D36—50：只根据证据选择一个最小修复；
- D51—60：对照、消融、统计与失败案例；
- D61—70：仓库、论文式报告、演示和答辩。

任何一天出现新想法，先写入 parking_lot，不立即改变主实验。
