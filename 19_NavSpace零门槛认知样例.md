# NavSpace 零门槛认知样例 V1

> 用时目标：45—60 分钟。目的不是先学会机器人或训练 VLA，而是看懂：一个导航 Agent 失败时，怎样区分“看错、想错、动作翻译错、评测接口错”。

## 一、把任务翻译成人话

NavSpace 给 Agent 一条自然语言指令和连续室内画面。我们准备优先审计的官方 LLM/SNav 提示路线要求模型从四种动作中选：

- `Move forward`：前进 0.25 米；
- `Turn left/right`：转 30 度；
- `Stop`：结束。

仓库的通用解析器实际上还识别 `backward/look up/look down`，数据/标注动作空间也更宽。这种“任务动作、prompt 声明和 parser 支持范围并不完全相同”的情况，正是需要记录而不能凭印象评价模型的协议因素。[通用解析器](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/common.py)

官方 benchmark 有六类空间任务、1,228 条轨迹，并统一评测 API 多模态模型、SNav 和 StreamVLN。[项目页](https://navspace.github.io/)｜[仓库](https://github.com/TidalHarley/NavSpace)

常用指标先只记三个：

- **SR**：最后停下时是否在目标附近；
- **OS/OSR**：途中是否曾经到过目标附近，即使后来又走开；
- **NE**：最终离目标还有多远。

如果 OS 高、SR 低，模型可能“到过但不会停”；这和完全不懂空间不是同一种失败。

## 二、一条假想轨迹

下面是**教学用例，不是官方已报告的某条 episode**。

指令：

> 向前经过灰色沙发，在墙上蓝色画作处左转，走到楼梯前停下。

### 观察序列

| 时刻 | 画面关键信息 | 理想动作 |
|---|---|---|
| t0 | 沙发在前方，画作还看不到 | 前进 |
| t1 | 已经过沙发，蓝色画作出现在左侧 | 左转 |
| t2 | 楼梯在前方约 1 米 | 前进一次 |
| t3 | 已在楼梯前目标区 | Stop |

真实运行时，模型输出：

```text
The painting is on my left, so I should rotate counter-clockwise.
```

从文字看，模型“看见了画作，也想对了方向”。但官方 prompt 要求受限动作格式，解析器也只识别有限短语。若提取不到任何动作，评测循环就强制 `stop`。于是 Agent 在 t1 原地结束，SR=0。[评测循环](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/simulation.py)｜[官方提示格式](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/prompts.py)

这次失败至少有两种解释：

1. 模型不懂空间；
2. 模型懂“向左”，但没有遵守动作协议。

只看 SR 无法区分两者。

## 三、四层失败分解

| 层 | 核心问题 | 例子 | 可观察证据 |
|---|---|---|---|
| 感知 | 看到了什么 | 没看见蓝色画作 | 静态问答也答错 |
| 空间推理 | 应往哪走 | 看见画作却判断应右转 | 能描述画面，但目标方向错 |
| 动作翻译 | 怎样把判断变成动作 | 认为应向左，却输出不受支持的表达 | 规范化后动作正确 |
| 评测协议 | 系统怎样读取和计分 | 无动作被解析后强制 Stop | 原始输出和执行动作不同 |

项目的核心不是给每次失败编故事，而是设计受控干预，使四层可以被区分。

官方 `common.py` 的代码注释还记录了一次已修复的 RGB/BGR 通道交换问题：旧 helper 会让视觉编码器看到红蓝互换的图像。这个已修复问题不能当作用户的新贡献，但它直接说明“评测实现细节会被误算成模型能力”，协议审计不是凭空担忧。[当前修复后的代码](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/common.py)

## 四、最小实验怎样做

### 条件 A：官方自由文本协议

要求模型严格输出：

```text
Final Answer: Move forward, Turn left, ...
```

保存原始输出、解析后的动作和实际执行动作。官方 SNav 路线本身还会均匀采样历史帧，并接受英文短语、箭头等有限表达；这些都是可测的接口变量。[SNav 评测代码](https://github.com/TidalHarley/NavSpace/blob/main/evaluation/eval_snav.py)

### 条件 B：结构化输出

同一模型、同一画面、同一指令，要求：

```json
{"actions": ["turn_left"]}
```

使用 schema 或枚举约束，不改变模型看到的空间信息。如果 B 明显减少非法/空动作，但静态空间问答不变，差异更可能来自动作协议。

### 条件 C：动作 oracle

研究者只把模型已经用自然语言说出的方向规范化为合法 token，例如 `rotate counter-clockwise`→`turn_left`。如果成功率上升，说明动作翻译/解析占一部分；这不是把正确答案直接送给模型。

### 条件 D：感知 oracle

额外提供一条真实空间事实，例如“蓝色画作当前位于左侧 60 度”。如果仍输出错误动作，问题更靠后；如果立刻恢复，问题更偏感知。

### 条件 E：帧采样

官方代码对过长历史做均匀采样。对照固定帧数下的：

- 均匀采样；
- 最近若干帧；
- 保留转向/新物体出现的关键帧。

只允许改变采样策略，不同时改 prompt、模型和动作解析。

## 五、结果应该长什么样

下面仍是**格式示例，不是测量结果**：

| 条件 | SR | OSR | 空动作率 | 非法动作率 | 到达后未停率 | 平均API调用 |
|---|---:|---:|---:|---:|---:|---:|
| 官方自由文本 | 12% | 21% | 8% | 6% | 9% | 11.4 |
| JSON schema | 16% | 22% | 0% | 0% | 6% | 11.2 |
| 动作规范化 oracle | 17% | 23% | 0% | 0% | 5% | 11.3 |
| 感知 oracle | 29% | 35% | 7% | 5% | 8% | 10.6 |

可能的解释：

- 12%→16/17% 表明接口确实造成一部分低分；
- 感知 oracle 提升更大，说明不能把主要问题都怪给 parser；
- OSR 比 SR 高，`stop` 仍是独立失效源。

必须在多个任务和至少两个模型上重复。如果只对一个模型、十条轨迹、一个 prompt 有效，只能算调试线索。

## 六、怎样从 L2 走到 L3

### L1：缩小复现

跑通一条 API 轨迹和一个固定小子集，确认 SR/OSR/NE 与日志可复验。

### L2：因果归因

用结构化输出、动作规范化、感知 oracle 和帧采样对照，把失败分解为感知、推理、动作翻译、协议/停止四类；报告置信区间和失败样例。

### L3：只修复证据指向的一层

例如数据证明 15% 以上失败来自“正确空间描述没有转成合法动作”，才设计轻量的感知—动作 contract：模型先输出结构化空间判断，再由确定性映射生成动作，并检查二者矛盾。

若主要失败来自感知，不能硬写一个 parser 当“创新”；应转向关键帧或视觉证据，但还要重新查新颖性。若所有提升只来自提示词措辞，项目退出。

## 七、为什么这不是普通 Demo

普通 Demo 会展示 Agent 成功走完一条路线。研究项目要回答：

1. 指标把哪些不同失败压成了同一个 0？
2. 哪个干预只改变一层，而没有偷看最终答案？
3. 结果能否跨模型、任务和随机运行保持？
4. 改进提高的是空间能力，还是只提高协议服从？

这类归因与 benchmark 可靠性、Agent 评测、多模态推理都有迁移性。它不需要真机，但比套一个现成 VLA 做视频更像研究。

## 八、答辩时三分钟怎么讲

> NavSpace 显示当前多模态模型在空间导航上成功率很低，但最终 SR 同时受到感知、空间推理、动作翻译和输出协议影响。我冻结模型和轨迹，分别对结构化动作、动作规范化、感知 oracle 与帧采样做受控干预，并保存原始输出、解析动作和执行轨迹。结果表明……的失败占比最高；因此我只针对该层提出……。该方法把……降低了……，但在……任务上无效，说明边界是……。

如果最后只能说“我让机器人在仿真里走起来了”，没有达到目标。

## 九、钱能与不能解决什么

- 能解决：API 调用、Linux/NVIDIA 服务器、本地 7B 模型推理、并行跑轨迹。
- 不能解决：HM3D 许可、Habitat 环境理解、干预是否真的只改变一层、提示词提升是否可泛化。

## 十、读完后的自检

1. 为什么“模型说向左，但 SR=0”不一定说明它不懂空间？
2. SR 和 OSR 差很多时，首先怀疑哪类问题？
3. 为什么 JSON 输出提高分数不等于空间智能提高？
4. 感知 oracle 和动作 oracle 分别隔离哪一层？
5. 你是否愿意连续数周看轨迹、画面和失败分类，而不是只看训练 loss？

前四题不会可以继续讲；第五题才是对工作形态的有效偏好。
