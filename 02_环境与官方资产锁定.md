# 环境与官方资产锁定

> 本文是开工前配置草案。命令尚未执行；模型 revision、云镜像和价格必须在正式开工当天重新核验。

## 一、官方事实

- 仓库：[PKU-Alignment/VLA-Arena](https://github.com/PKU-Alignment/VLA-Arena)
- 2026-08-25 候选提交：[babe582](https://github.com/PKU-Alignment/VLA-Arena/commit/babe582ebffc82b979b77964a7e56417d02f63a4)
- 官方要求：Python 3.11，Ubuntu 20.04+，GPU 路径要求 CUDA 11.8+；
- 仓库按模型使用独立 uv 环境；
- 官方 README 对套件使用了短名 preposition_combinations，但代码注册表、BDDL目录和 init 文件使用 extrapolation_preposition_combinations。执行配置以代码注册名为准；
- 每个 level 有5项任务；官方评测配置默认每项10次 trial；论文完整评测口径需在复现实验表中另行标注。

## 二、首台云实例

推荐起步规格：

| 资源 | 推荐 | 原因 |
|---|---|---|
| GPU | 1 × A100 80GB；H100 80GB可替代 | 先避免量化和显存把模型能力混入实验变量 |
| CPU | 16—32 vCPU | 仿真、视频编码和数据加载 |
| 内存 | 64—128GB | OpenVLA、仿真和缓存余量 |
| 系统盘/数据盘 | 至少200GB SSD，可扩到300GB | 仓库、uv缓存、两个模型、日志和视频 |
| 系统 | Ubuntu 22.04 LTS | 满足官方 Ubuntu 20.04+，生态较稳 |
| 图形 | NVIDIA EGL/OpenGL 无头渲染可用 | 仿真能否离屏渲染是硬门槛 |
| 网络 | 可稳定访问 GitHub 与 Hugging Face | 模型和依赖下载 |

A6000 48GB 可用于 SmolVLA 或后续轻量训练，但不作为第一台主诊断机器的默认值。多卡只在 D14 以后且训练方案确实需要时申请。

## 三、资产顺序

| 顺序 | 资产 | 用途 | 当前动作 |
|---:|---|---|---|
| 1 | 仓库候选提交 | 代码与任务 | 只记录，不克隆 |
| 2 | base/random 路径 | 隔离验证仿真和渲染 | D2 |
| 3 | VLA-Arena/smolvla-vla-arena | 快速验证完整 VLA 链 | D3 |
| 4 | VLA-Arena/openvla-7b-finetuned-vla-arena | 获得更有用的 L0 成功样本 | D7 |
| 5 | L0 训练数据 | 只在诊断通过、确需训练时下载 | D14以后 |

不要在第一天同时下载所有模型和完整数据集。

## 四、Day 0 版本冻结

1. 获取届时 main 的提交号；
2. 比较它与 babe582 的提交记录和以下关键路径：
   - vla_arena/configs/evaluation
   - vla_arena/models/smolvla/evaluator.py
   - vla_arena/models/openvla/evaluator.py
   - vla_arena/vla_arena/benchmark
   - vla_arena/vla_arena/bddl_files/extrapolation_preposition_combinations
3. 若只是文档或无关任务变化，继续使用候选提交；
4. 若修复了目标套件、模型加载、初始状态或成功判定，审阅后更新；
5. 把最终仓库 SHA、模型 revision、数据 revision 写入 environment.md 和每条实验记录。

## 五、命令草案

以下命令只作开工清单，届时逐条执行和记录，不整段盲贴：

    git clone https://github.com/PKU-Alignment/VLA-Arena.git
    cd VLA-Arena
    git checkout babe582ebffc82b979b77964a7e56417d02f63a4
    uv sync --project envs/base

模型环境：

    uv sync --project envs/smolvla
    uv sync --project envs/openvla

评测命令形式：

    uv run --project envs/smolvla vla-arena eval --model smolvla --config <自定义配置>
    uv run --project envs/openvla vla-arena eval --model openvla --config <自定义配置>

不得直接修改官方 YAML。复制到项目自己的 configs 目录，并把以下字段显式写入：

- task_suite_name: extrapolation_preposition_combinations
- task_level: 0、1 或2
- num_trials_per_task: pilot先1或5，正式基线再扩大
- init_state_selection_mode 与 offset
- seed
- result_json_path、local_log_dir、video_out_path
- use_replacements: false，除非该次实验明确研究语言替换

## 六、建议项目目录

    VLA-RelComp/
      upstream/VLA-Arena/
      configs/
      manifests/
      src/instrumentation/
      src/interventions/
      scripts/
      experiments/registry/
      experiments/logs/
      experiments/videos/
      experiments/results/
      reports/figures/
      reports/tables/
      notes/daily/
      notes/gates/
      parking_lot.md

upstream 只保存官方代码和明确补丁；实验结果、配置和分析保存在主项目目录，避免拉取上游更新时混乱。

## 七、安全与成本纪律

- 不把云密钥、Hugging Face token 或个人信息写进仓库；
- 每次关机前确认日志和小文件已同步；大型模型缓存不重复上传；
- 每个实验先用1个 episode 做 smoke test，再扩大；
- 每天记录 GPU 小时、峰值显存、墙钟时间和磁盘增量；
- D8 用实测吞吐重算总预算；在此之前不承诺精确费用；
- 不因用户愿意付费就跳过实验设计。钱可以换算力，不能修复数据泄漏、不可重复和认知失控。
