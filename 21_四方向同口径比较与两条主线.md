# 四方向同口径比较与两条主线 V1

> 快照：2026-08-25。本轮只解决“先往哪两条主线继续找项目”，不选择最终论文、仓库或导师，不安装环境，不把本机算力当淘汰条件。

## 一、结论先行

后续不再平均调查所有 AI 方向，压成两个项目设计主线：

1. **主线 A：多模态模型能力提升——视觉理解/推理 × 轻量后训练。**
2. **主线 B：具身 VLA——以 RoboTwin 等公开仿真平台为载体，研究操作策略的泛化或可靠性。**

两个保留项：

- **文本 LLM 后训练**不独立占一个最终席位，而是主线 A 的低风险技术底座和备用落点。若多模态训练链过重，可以退到同一套 SFT/LoRA/RLVR/评测方法的文本小模型版本，不必推倒重学。
- **视觉生成/世界模型**暂不独立开线。生成模型可作为技术参考；世界模型若能直接服务 VLA 的预测和决策，再并入主线 B。单纯“生成更好看/缓存更快”目前不是优先目标。

软件 Agent、AI 系统和芯片本轮不作为主方向继续扩张。它们不是没有价值，而是与用户当前希望积累的“模型、多模态、具身”能力不够一致。

## 二、为什么不是继续查更多方向

已有北大师资库足以确认四条线都真实存在且近期活跃：

- 基础模型/后训练：贺笛、张牧涵、张驰、潘亮铭等；
- 多模态/视觉：刘家瑛、彭宇新、杨帅、张史梁、钟亦武、周嘉欢、唐浩等；
- 具身/VLA：董豪、王鹤、卢宗青、杨耀东、仉尚航、刘航欣等；
- 视觉生成/世界模型：与多模态、具身两边都有交叉。

外部校准也显示它们都不是过时方向。ICLR 2026 官方工作坊同时出现 Scaling Post-training、Multimodal Intelligence、World Models、Agent Memory 和 Reliable Autonomy；ICLR 2026 正式论文也继续研究 SFT/RL 的关系、多模态 RLVR、VLA-RL 和闭环世界模型。[ICLR 2026 工作坊](https://blog.iclr.cc/2026/01/13/iclr2026-workshops/)｜[多模态 RLVR 示例](https://proceedings.iclr.cc/paper_files/paper/2026/hash/82846e19e6d42ebfd4ace4361def29ae-Abstract-Conference.html)｜[VLA-RL 示例](https://proceedings.iclr.cc/paper_files/paper/2026/hash/cbfbcb4da14235bd69b134070898ae9d-Abstract-Conference.html)｜[闭环世界模型示例](https://proceedings.iclr.cc/paper_files/paper/2026/hash/5b4263be85820683d78675cc18d2efc7-Abstract-Conference.html)

因此继续扩大方向池的边际价值已经很低。现在该比较工作形态与70天上限，而不是再证明它们“很热”。

## 三、统一权重

方向分只用于收敛，不冒充录用概率。

| 维度 | 权重 | 口径 |
|---|---:|---|
| 70天形成L3研究闭环 | 25 | 能否从基线走到自己的假设、改动、对照、消融和失败边界 |
| 长期知识迁移 | 20 | 热词退潮后，知识能否迁往其他模型、任务或行业 |
| 当前前沿与学术上限 | 20 | 近年是否仍有高水平问题和明显未解瓶颈 |
| 零基础认知可掌握性 | 15 | 不是“代码能否生成”，而是用户能否在复试前讲透 |
| 公开生态与实验抓手 | 10 | 模型、代码、数据、benchmark和缩小实验是否存在 |
| 北大方向生态 | 5 | 只看方向覆盖，暂不把具体导师风评混入 |
| 演示与表达价值 | 5 | 是否容易把问题、失败和改进清楚展示 |

## 四、四方向横向结果

| 方向 | 闭环25 | 迁移20 | 前沿20 | 掌握15 | 生态10 | 北大5 | 展示5 | 合计 | 本轮角色 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 多模态理解/推理 × 后训练 | 20 | 18 | 19 | 10 | 9 | 5 | 5 | **86** | 主线A |
| 文本LLM后训练/推理 | 21 | 17 | 19 | 11 | 10 | 4 | 3 | **85** | A的底座/低风险备用 |
| 具身VLA/机器人学习 | 14 | 19 | 20 | 6 | 8 | 5 | 5 | **77** | 主线B，高风险高上限 |
| 视觉生成/世界模型 | 15 | 17 | 18 | 8 | 8 | 4 | 5 | **75** | 并入B或保留参考 |

这不是“只取总分前二”。如果只按成功率，会留下多模态和文本 LLM，两者的技术栈和项目叙事又过于接近，无法检验具身这条高上限路线。最终采用的是风险组合：

- 主线 A 提供较高成功率、广泛模型知识和清晰退路；
- 主线 B 检验更具体的物理任务、北大具身生态和更高展示/研究上限；
- 文本 LLM 是 A 的可降级版本，而不是第三套完全不同的学习负担。

## 五、四条方向到底在做什么

### 1. 文本 LLM 后训练与推理

**真实问题**：预训练模型“见过很多”，但未必会稳定地推理、服从约束或使用合适的解题策略。SFT、偏好优化、RLVR等方法改变模型的行为分布。

**零门槛例子**：同一个3B模型先做一小段数学SFT，再做可验证奖励RL。问题不是“准确率涨没涨”这么简单，而是SFT学得太久会不会让模型答案更整齐，却失去探索不同解法的能力。ICLR 2026已有工作表明，SFT阶段的高分并不稳定预示后续RL效果，说明这里仍有真实机制问题。[SFT-RL关系示例](https://proceedings.iclr.cc/paper_files/paper/2026/hash/5d6ae8ba43ecb378030753c4408ef9bd-Abstract-Conference.html)

**70天可达上限**：在1.5B—7B开源模型上做LoRA/SFT或缩小RLVR，研究数据组成、早停、奖励或泛化。可以达到L3，但竞争非常拥挤，必须有明确问题，不能只是“我也训了一个模型”。

**长期资产**：Transformer、PyTorch、训练/验证、数据、优化、模型评测，迁移性最广。

### 2. 多模态理解/推理 × 后训练

**真实问题**：模型会生成流畅推理，却可能根本没看对图；文字推理能力和视觉感知能力没有真正结合。

**零门槛例子**：一张图中小球在杯子左边，模型最后答案正确，但理由一直把左右说反。项目可以研究：训练奖励只看最终答案时，是否鼓励了“猜对但看错”；加入视觉依赖约束或对照扰动后，能不能真正减少感知错误。

**70天可达上限**：固定2B—7B开源VLM和一个小型视觉推理任务，先复现，再做轻量SFT/LoRA/小规模RL或数据干预，比较答案、视觉落点、推理忠实性和域外泛化。比纯文本多一段视觉链，但仍可缩小。

**长期资产**：继承文本后训练全部基础，再增加视觉编码、跨模态对齐、grounding和多模态评测。以后可迁往视频、具身、自动驾驶和内容理解。

**主要风险**：只换prompt、只列失败案例或只提高一个benchmark分数，含金量不足；正式切口还要做撞题审计。

### 3. 具身VLA

**真实问题**：模型不仅回答，还要把图像和语言变成连续动作；一个微小误差可能在环境交互中累积成失败。

**零门槛例子**：指令是“双手把两个瓶子放入篮子”。模型需要看见瓶子和篮子、决定先后顺序、输出两条机械臂的动作，并根据新画面继续纠错。它可能不是“完全不懂”，而是在遮挡、物体位置变化或动作误差后无法恢复。

RoboTwin 2.0正好提供50个双臂任务、5种机器人形态、数据和统一评测；官方也把单任务微调、视觉/语言鲁棒性、多任务和跨形态能力列为研究入口。它不是北大项目，但可作为公开实验平台。[RoboTwin项目页](https://robotwin-platform.github.io/)｜[官方仓库](https://github.com/RoboTwin-Platform/RoboTwin)

**70天可达上限**：可以做到“已有VLA＋少量任务＋一种明确变化＋小规模适配/改进＋多seed评测”；不适合从零预训练通用VLA、全50任务、真机和新架构同时进行。

**长期资产**：多模态、策略学习、空间理解、闭环决策、鲁棒性；底层问题比某一代VLA名字更耐久。

**主要风险**：仿真、数据、模型服务、动作空间和评测链同时存在，认知与调试时间不能用更多GPU完全买掉。它保留为高上限主线，但必须有一个14天退出闸门。

### 4. 视觉生成与世界模型

这其实包含两种不同工作：

- **视觉生成**关注图像/视频是否更快、更可控、更一致；
- **世界模型**关注模型能否预测动作后的未来，并帮助决策。

仅做视频生成缓存虽然直观，但MagCache等工作已经把训练免费加速推进得很快，容易落入拥挤的小技巧竞争。[MagCache，NeurIPS 2025](https://proceedings.neurips.cc/paper_files/paper/2025/hash/311207bb626e36a8f1d3eb92aa67af22-Abstract-Conference.html)

世界模型更有长期意义，但独立训练的门槛高。ICLR 2026的闭环结果还提示：视觉看起来逼真不等于能帮助任务，真正关键的是可控性和闭环成功率。因此本轮不单独开“生成项目”，只在它能服务VLA预测、数据或决策时并入主线B。

## 六、下一步项目设计边界

下一轮只产生四种项目形态，不再全网撒网：

| 主线 | 项目形态1 | 项目形态2 |
|---|---|---|
| A 多模态模型能力 | 小VLM的视觉推理/grounding后训练 | 感知错误、推理忠实性与域外泛化的诊断＋针对性改进 |
| B 具身VLA | RoboTwin少量任务上的VLA适配与泛化 | 受控扰动或失败恢复下的策略改进 |

每种形态只找1—2个近期正式论文/开源基线，合计不超过6个入口。下一轮先做书面新颖性、知识负担和70天切口，不跑GPU。只有压到两个具体候选后，才查对应教师、团队风评、许可和仓库执行性。

## 七、本轮是否足以继续

足够。现在已经回答：

- 方向没有只剩软件Agent和导航；
- RoboTwin能进入候选，但不冒充北大成果；
- VLA不是完全做不了，只能严格限定到已有模型、小任务和小改进；
- 文本后训练是低风险公共底座，多模态是优先模型主线；
- 具身是高风险高上限主线；
- 世界模型暂时作为两者交叉，不另起一套庞大学习链。

因此可以直接进入“四种项目形态→不超过六个具体入口”的项目设计，不需要用户先回答专业问题。
